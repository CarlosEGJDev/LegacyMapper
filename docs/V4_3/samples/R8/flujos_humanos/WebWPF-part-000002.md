# Documentación humana de flujos — WebWPF

*Esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R4`. [Volver al índice](../HUMAN_DOCUMENTATION.md).*

12 flujo(s) en este documento.

# Flujo FLOW-00089: proyectos\WebWPF\Formulario89.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario89.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_89` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00089`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_89`).

### Trazabilidad por camino

- `PATH` `PATH-00089` — evidencia: (sin referencias de evidencia)
  - `PATH-00089` → `index/functional_paths.json#PATH-00089`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00089`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00009: proyectos\WebWPF\Formulario9.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario9.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_9` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00009`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_9`).

### Trazabilidad por camino

- `PATH` `PATH-00009` — evidencia: (sin referencias de evidencia)
  - `PATH-00009` → `index/functional_paths.json#PATH-00009`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00009`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00090: proyectos\WebWPF\Formulario90.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario90.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_90` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00090`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_90`).

### Trazabilidad por camino

- `PATH` `PATH-00090` — evidencia: (sin referencias de evidencia)
  - `PATH-00090` → `index/functional_paths.json#PATH-00090`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00090`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00091: proyectos\WebWPF\Formulario91.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario91.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_91` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00091`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_91`).

### Trazabilidad por camino

- `PATH` `PATH-00091` — evidencia: (sin referencias de evidencia)
  - `PATH-00091` → `index/functional_paths.json#PATH-00091`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00091`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00092: proyectos\WebWPF\Formulario92.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario92.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_92` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00092`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_92`).

### Trazabilidad por camino

- `PATH` `PATH-00092` — evidencia: (sin referencias de evidencia)
  - `PATH-00092` → `index/functional_paths.json#PATH-00092`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00092`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00093: proyectos\WebWPF\Formulario93.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario93.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_93` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00093`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_93`).

### Trazabilidad por camino

- `PATH` `PATH-00093` — evidencia: (sin referencias de evidencia)
  - `PATH-00093` → `index/functional_paths.json#PATH-00093`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00093`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00094: proyectos\WebWPF\Formulario94.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario94.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_94` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00094`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_94`).

### Trazabilidad por camino

- `PATH` `PATH-00094` — evidencia: (sin referencias de evidencia)
  - `PATH-00094` → `index/functional_paths.json#PATH-00094`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00094`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00095: proyectos\WebWPF\Formulario95.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario95.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_95` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00095`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_95`).

### Trazabilidad por camino

- `PATH` `PATH-00095` — evidencia: (sin referencias de evidencia)
  - `PATH-00095` → `index/functional_paths.json#PATH-00095`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00095`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00096: proyectos\WebWPF\Formulario96.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario96.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_96` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00096`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_96`).

### Trazabilidad por camino

- `PATH` `PATH-00096` — evidencia: (sin referencias de evidencia)
  - `PATH-00096` → `index/functional_paths.json#PATH-00096`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00096`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00097: proyectos\WebWPF\Formulario97.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario97.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_97` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00097`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_97`).

### Trazabilidad por camino

- `PATH` `PATH-00097` — evidencia: (sin referencias de evidencia)
  - `PATH-00097` → `index/functional_paths.json#PATH-00097`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00097`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00098: proyectos\WebWPF\Formulario98.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario98.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_98` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00098`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_98`).

### Trazabilidad por camino

- `PATH` `PATH-00098` — evidencia: (sin referencias de evidencia)
  - `PATH-00098` → `index/functional_paths.json#PATH-00098`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00098`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00099: proyectos\WebWPF\Formulario99.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario99.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_99` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00099`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_99`).

### Trazabilidad por camino

- `PATH` `PATH-00099` — evidencia: (sin referencias de evidencia)
  - `PATH-00099` → `index/functional_paths.json#PATH-00099`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00099`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

