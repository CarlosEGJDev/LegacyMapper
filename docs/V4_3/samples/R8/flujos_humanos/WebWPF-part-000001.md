# Documentación humana de flujos — WebWPF

*Esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R4`. [Volver al índice](../HUMAN_DOCUMENTATION.md).*

500 flujo(s) en este documento.

# Flujo FLOW-00000: proyectos\WebWPF\Formulario0.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario0.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_0` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00000`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_0`).

### Trazabilidad por camino

- `PATH` `PATH-00000` — evidencia: (sin referencias de evidencia)
  - `PATH-00000` → `index/functional_paths.json#PATH-00000`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00000`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00001: proyectos\WebWPF\Formulario1.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario1.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_1` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00001`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_1`).

### Trazabilidad por camino

- `PATH` `PATH-00001` — evidencia: (sin referencias de evidencia)
  - `PATH-00001` → `index/functional_paths.json#PATH-00001`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00001`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00010: proyectos\WebWPF\Formulario10.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario10.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_10` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00010`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_10`).

### Trazabilidad por camino

- `PATH` `PATH-00010` — evidencia: (sin referencias de evidencia)
  - `PATH-00010` → `index/functional_paths.json#PATH-00010`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00010`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00100: proyectos\WebWPF\Formulario100.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario100.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_100` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00100`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_100`).

### Trazabilidad por camino

- `PATH` `PATH-00100` — evidencia: (sin referencias de evidencia)
  - `PATH-00100` → `index/functional_paths.json#PATH-00100`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00100`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00101: proyectos\WebWPF\Formulario101.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario101.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_101` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00101`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_101`).

### Trazabilidad por camino

- `PATH` `PATH-00101` — evidencia: (sin referencias de evidencia)
  - `PATH-00101` → `index/functional_paths.json#PATH-00101`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00101`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00102: proyectos\WebWPF\Formulario102.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario102.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_102` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00102`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_102`).

### Trazabilidad por camino

- `PATH` `PATH-00102` — evidencia: (sin referencias de evidencia)
  - `PATH-00102` → `index/functional_paths.json#PATH-00102`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00102`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00103: proyectos\WebWPF\Formulario103.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario103.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_103` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00103`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_103`).

### Trazabilidad por camino

- `PATH` `PATH-00103` — evidencia: (sin referencias de evidencia)
  - `PATH-00103` → `index/functional_paths.json#PATH-00103`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00103`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00104: proyectos\WebWPF\Formulario104.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario104.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_104` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00104`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_104`).

### Trazabilidad por camino

- `PATH` `PATH-00104` — evidencia: (sin referencias de evidencia)
  - `PATH-00104` → `index/functional_paths.json#PATH-00104`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00104`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00105: proyectos\WebWPF\Formulario105.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario105.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_105` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00105`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_105`).

### Trazabilidad por camino

- `PATH` `PATH-00105` — evidencia: (sin referencias de evidencia)
  - `PATH-00105` → `index/functional_paths.json#PATH-00105`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00105`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00106: proyectos\WebWPF\Formulario106.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario106.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_106` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00106`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_106`).

### Trazabilidad por camino

- `PATH` `PATH-00106` — evidencia: (sin referencias de evidencia)
  - `PATH-00106` → `index/functional_paths.json#PATH-00106`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00106`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00107: proyectos\WebWPF\Formulario107.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario107.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_107` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00107`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_107`).

### Trazabilidad por camino

- `PATH` `PATH-00107` — evidencia: (sin referencias de evidencia)
  - `PATH-00107` → `index/functional_paths.json#PATH-00107`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00107`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00108: proyectos\WebWPF\Formulario108.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario108.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_108` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00108`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_108`).

### Trazabilidad por camino

- `PATH` `PATH-00108` — evidencia: (sin referencias de evidencia)
  - `PATH-00108` → `index/functional_paths.json#PATH-00108`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00108`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00109: proyectos\WebWPF\Formulario109.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario109.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_109` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00109`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_109`).

### Trazabilidad por camino

- `PATH` `PATH-00109` — evidencia: (sin referencias de evidencia)
  - `PATH-00109` → `index/functional_paths.json#PATH-00109`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00109`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00011: proyectos\WebWPF\Formulario11.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario11.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_11` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00011`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_11`).

### Trazabilidad por camino

- `PATH` `PATH-00011` — evidencia: (sin referencias de evidencia)
  - `PATH-00011` → `index/functional_paths.json#PATH-00011`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00011`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00110: proyectos\WebWPF\Formulario110.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario110.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_110` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00110`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_110`).

### Trazabilidad por camino

- `PATH` `PATH-00110` — evidencia: (sin referencias de evidencia)
  - `PATH-00110` → `index/functional_paths.json#PATH-00110`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00110`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00111: proyectos\WebWPF\Formulario111.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario111.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_111` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00111`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_111`).

### Trazabilidad por camino

- `PATH` `PATH-00111` — evidencia: (sin referencias de evidencia)
  - `PATH-00111` → `index/functional_paths.json#PATH-00111`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00111`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00112: proyectos\WebWPF\Formulario112.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario112.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_112` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00112`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_112`).

### Trazabilidad por camino

- `PATH` `PATH-00112` — evidencia: (sin referencias de evidencia)
  - `PATH-00112` → `index/functional_paths.json#PATH-00112`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00112`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00113: proyectos\WebWPF\Formulario113.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario113.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_113` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00113`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_113`).

### Trazabilidad por camino

- `PATH` `PATH-00113` — evidencia: (sin referencias de evidencia)
  - `PATH-00113` → `index/functional_paths.json#PATH-00113`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00113`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00114: proyectos\WebWPF\Formulario114.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario114.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_114` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00114`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_114`).

### Trazabilidad por camino

- `PATH` `PATH-00114` — evidencia: (sin referencias de evidencia)
  - `PATH-00114` → `index/functional_paths.json#PATH-00114`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00114`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00115: proyectos\WebWPF\Formulario115.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario115.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_115` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00115`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_115`).

### Trazabilidad por camino

- `PATH` `PATH-00115` — evidencia: (sin referencias de evidencia)
  - `PATH-00115` → `index/functional_paths.json#PATH-00115`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00115`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00116: proyectos\WebWPF\Formulario116.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario116.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_116` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00116`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_116`).

### Trazabilidad por camino

- `PATH` `PATH-00116` — evidencia: (sin referencias de evidencia)
  - `PATH-00116` → `index/functional_paths.json#PATH-00116`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00116`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00117: proyectos\WebWPF\Formulario117.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario117.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_117` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00117`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_117`).

### Trazabilidad por camino

- `PATH` `PATH-00117` — evidencia: (sin referencias de evidencia)
  - `PATH-00117` → `index/functional_paths.json#PATH-00117`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00117`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00118: proyectos\WebWPF\Formulario118.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario118.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_118` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00118`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_118`).

### Trazabilidad por camino

- `PATH` `PATH-00118` — evidencia: (sin referencias de evidencia)
  - `PATH-00118` → `index/functional_paths.json#PATH-00118`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00118`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00119: proyectos\WebWPF\Formulario119.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario119.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_119` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00119`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_119`).

### Trazabilidad por camino

- `PATH` `PATH-00119` — evidencia: (sin referencias de evidencia)
  - `PATH-00119` → `index/functional_paths.json#PATH-00119`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00119`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00012: proyectos\WebWPF\Formulario12.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario12.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_12` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00012`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_12`).

### Trazabilidad por camino

- `PATH` `PATH-00012` — evidencia: (sin referencias de evidencia)
  - `PATH-00012` → `index/functional_paths.json#PATH-00012`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00012`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00120: proyectos\WebWPF\Formulario120.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario120.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_120` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00120`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_120`).

### Trazabilidad por camino

- `PATH` `PATH-00120` — evidencia: (sin referencias de evidencia)
  - `PATH-00120` → `index/functional_paths.json#PATH-00120`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00120`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00121: proyectos\WebWPF\Formulario121.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario121.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_121` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00121`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_121`).

### Trazabilidad por camino

- `PATH` `PATH-00121` — evidencia: (sin referencias de evidencia)
  - `PATH-00121` → `index/functional_paths.json#PATH-00121`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00121`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00122: proyectos\WebWPF\Formulario122.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario122.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_122` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00122`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_122`).

### Trazabilidad por camino

- `PATH` `PATH-00122` — evidencia: (sin referencias de evidencia)
  - `PATH-00122` → `index/functional_paths.json#PATH-00122`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00122`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00123: proyectos\WebWPF\Formulario123.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario123.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_123` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00123`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_123`).

### Trazabilidad por camino

- `PATH` `PATH-00123` — evidencia: (sin referencias de evidencia)
  - `PATH-00123` → `index/functional_paths.json#PATH-00123`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00123`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00124: proyectos\WebWPF\Formulario124.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario124.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_124` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00124`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_124`).

### Trazabilidad por camino

- `PATH` `PATH-00124` — evidencia: (sin referencias de evidencia)
  - `PATH-00124` → `index/functional_paths.json#PATH-00124`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00124`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00125: proyectos\WebWPF\Formulario125.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario125.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_125` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00125`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_125`).

### Trazabilidad por camino

- `PATH` `PATH-00125` — evidencia: (sin referencias de evidencia)
  - `PATH-00125` → `index/functional_paths.json#PATH-00125`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00125`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00126: proyectos\WebWPF\Formulario126.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario126.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_126` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00126`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_126`).

### Trazabilidad por camino

- `PATH` `PATH-00126` — evidencia: (sin referencias de evidencia)
  - `PATH-00126` → `index/functional_paths.json#PATH-00126`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00126`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00127: proyectos\WebWPF\Formulario127.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario127.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_127` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00127`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_127`).

### Trazabilidad por camino

- `PATH` `PATH-00127` — evidencia: (sin referencias de evidencia)
  - `PATH-00127` → `index/functional_paths.json#PATH-00127`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00127`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00128: proyectos\WebWPF\Formulario128.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario128.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_128` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00128`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_128`).

### Trazabilidad por camino

- `PATH` `PATH-00128` — evidencia: (sin referencias de evidencia)
  - `PATH-00128` → `index/functional_paths.json#PATH-00128`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00128`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00129: proyectos\WebWPF\Formulario129.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario129.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_129` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00129`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_129`).

### Trazabilidad por camino

- `PATH` `PATH-00129` — evidencia: (sin referencias de evidencia)
  - `PATH-00129` → `index/functional_paths.json#PATH-00129`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00129`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00013: proyectos\WebWPF\Formulario13.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario13.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_13` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00013`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_13`).

### Trazabilidad por camino

- `PATH` `PATH-00013` — evidencia: (sin referencias de evidencia)
  - `PATH-00013` → `index/functional_paths.json#PATH-00013`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00013`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00130: proyectos\WebWPF\Formulario130.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario130.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_130` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00130`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_130`).

### Trazabilidad por camino

- `PATH` `PATH-00130` — evidencia: (sin referencias de evidencia)
  - `PATH-00130` → `index/functional_paths.json#PATH-00130`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00130`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00131: proyectos\WebWPF\Formulario131.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario131.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_131` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00131`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_131`).

### Trazabilidad por camino

- `PATH` `PATH-00131` — evidencia: (sin referencias de evidencia)
  - `PATH-00131` → `index/functional_paths.json#PATH-00131`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00131`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00132: proyectos\WebWPF\Formulario132.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario132.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_132` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00132`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_132`).

### Trazabilidad por camino

- `PATH` `PATH-00132` — evidencia: (sin referencias de evidencia)
  - `PATH-00132` → `index/functional_paths.json#PATH-00132`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00132`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00133: proyectos\WebWPF\Formulario133.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario133.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_133` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00133`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_133`).

### Trazabilidad por camino

- `PATH` `PATH-00133` — evidencia: (sin referencias de evidencia)
  - `PATH-00133` → `index/functional_paths.json#PATH-00133`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00133`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00134: proyectos\WebWPF\Formulario134.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario134.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_134` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00134`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_134`).

### Trazabilidad por camino

- `PATH` `PATH-00134` — evidencia: (sin referencias de evidencia)
  - `PATH-00134` → `index/functional_paths.json#PATH-00134`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00134`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00135: proyectos\WebWPF\Formulario135.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario135.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_135` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00135`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_135`).

### Trazabilidad por camino

- `PATH` `PATH-00135` — evidencia: (sin referencias de evidencia)
  - `PATH-00135` → `index/functional_paths.json#PATH-00135`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00135`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00136: proyectos\WebWPF\Formulario136.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario136.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_136` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00136`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_136`).

### Trazabilidad por camino

- `PATH` `PATH-00136` — evidencia: (sin referencias de evidencia)
  - `PATH-00136` → `index/functional_paths.json#PATH-00136`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00136`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00137: proyectos\WebWPF\Formulario137.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario137.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_137` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00137`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_137`).

### Trazabilidad por camino

- `PATH` `PATH-00137` — evidencia: (sin referencias de evidencia)
  - `PATH-00137` → `index/functional_paths.json#PATH-00137`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00137`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00138: proyectos\WebWPF\Formulario138.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario138.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_138` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00138`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_138`).

### Trazabilidad por camino

- `PATH` `PATH-00138` — evidencia: (sin referencias de evidencia)
  - `PATH-00138` → `index/functional_paths.json#PATH-00138`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00138`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00139: proyectos\WebWPF\Formulario139.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario139.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_139` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00139`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_139`).

### Trazabilidad por camino

- `PATH` `PATH-00139` — evidencia: (sin referencias de evidencia)
  - `PATH-00139` → `index/functional_paths.json#PATH-00139`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00139`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00014: proyectos\WebWPF\Formulario14.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario14.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_14` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00014`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_14`).

### Trazabilidad por camino

- `PATH` `PATH-00014` — evidencia: (sin referencias de evidencia)
  - `PATH-00014` → `index/functional_paths.json#PATH-00014`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00014`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00140: proyectos\WebWPF\Formulario140.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario140.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_140` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00140`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_140`).

### Trazabilidad por camino

- `PATH` `PATH-00140` — evidencia: (sin referencias de evidencia)
  - `PATH-00140` → `index/functional_paths.json#PATH-00140`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00140`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00141: proyectos\WebWPF\Formulario141.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario141.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_141` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00141`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_141`).

### Trazabilidad por camino

- `PATH` `PATH-00141` — evidencia: (sin referencias de evidencia)
  - `PATH-00141` → `index/functional_paths.json#PATH-00141`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00141`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00142: proyectos\WebWPF\Formulario142.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario142.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_142` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00142`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_142`).

### Trazabilidad por camino

- `PATH` `PATH-00142` — evidencia: (sin referencias de evidencia)
  - `PATH-00142` → `index/functional_paths.json#PATH-00142`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00142`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00143: proyectos\WebWPF\Formulario143.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario143.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_143` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00143`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_143`).

### Trazabilidad por camino

- `PATH` `PATH-00143` — evidencia: (sin referencias de evidencia)
  - `PATH-00143` → `index/functional_paths.json#PATH-00143`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00143`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00144: proyectos\WebWPF\Formulario144.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario144.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_144` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00144`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_144`).

### Trazabilidad por camino

- `PATH` `PATH-00144` — evidencia: (sin referencias de evidencia)
  - `PATH-00144` → `index/functional_paths.json#PATH-00144`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00144`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00145: proyectos\WebWPF\Formulario145.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario145.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_145` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00145`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_145`).

### Trazabilidad por camino

- `PATH` `PATH-00145` — evidencia: (sin referencias de evidencia)
  - `PATH-00145` → `index/functional_paths.json#PATH-00145`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00145`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00146: proyectos\WebWPF\Formulario146.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario146.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_146` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00146`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_146`).

### Trazabilidad por camino

- `PATH` `PATH-00146` — evidencia: (sin referencias de evidencia)
  - `PATH-00146` → `index/functional_paths.json#PATH-00146`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00146`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00147: proyectos\WebWPF\Formulario147.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario147.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_147` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00147`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_147`).

### Trazabilidad por camino

- `PATH` `PATH-00147` — evidencia: (sin referencias de evidencia)
  - `PATH-00147` → `index/functional_paths.json#PATH-00147`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00147`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00148: proyectos\WebWPF\Formulario148.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario148.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_148` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00148`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_148`).

### Trazabilidad por camino

- `PATH` `PATH-00148` — evidencia: (sin referencias de evidencia)
  - `PATH-00148` → `index/functional_paths.json#PATH-00148`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00148`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00149: proyectos\WebWPF\Formulario149.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario149.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_149` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00149`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_149`).

### Trazabilidad por camino

- `PATH` `PATH-00149` — evidencia: (sin referencias de evidencia)
  - `PATH-00149` → `index/functional_paths.json#PATH-00149`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00149`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00015: proyectos\WebWPF\Formulario15.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario15.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_15` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00015`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_15`).

### Trazabilidad por camino

- `PATH` `PATH-00015` — evidencia: (sin referencias de evidencia)
  - `PATH-00015` → `index/functional_paths.json#PATH-00015`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00015`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00150: proyectos\WebWPF\Formulario150.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario150.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_150` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00150`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_150`).

### Trazabilidad por camino

- `PATH` `PATH-00150` — evidencia: (sin referencias de evidencia)
  - `PATH-00150` → `index/functional_paths.json#PATH-00150`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00150`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00151: proyectos\WebWPF\Formulario151.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario151.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_151` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00151`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_151`).

### Trazabilidad por camino

- `PATH` `PATH-00151` — evidencia: (sin referencias de evidencia)
  - `PATH-00151` → `index/functional_paths.json#PATH-00151`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00151`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00152: proyectos\WebWPF\Formulario152.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario152.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_152` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00152`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_152`).

### Trazabilidad por camino

- `PATH` `PATH-00152` — evidencia: (sin referencias de evidencia)
  - `PATH-00152` → `index/functional_paths.json#PATH-00152`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00152`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00153: proyectos\WebWPF\Formulario153.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario153.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_153` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00153`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_153`).

### Trazabilidad por camino

- `PATH` `PATH-00153` — evidencia: (sin referencias de evidencia)
  - `PATH-00153` → `index/functional_paths.json#PATH-00153`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00153`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00154: proyectos\WebWPF\Formulario154.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario154.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_154` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00154`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_154`).

### Trazabilidad por camino

- `PATH` `PATH-00154` — evidencia: (sin referencias de evidencia)
  - `PATH-00154` → `index/functional_paths.json#PATH-00154`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00154`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00155: proyectos\WebWPF\Formulario155.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario155.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_155` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00155`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_155`).

### Trazabilidad por camino

- `PATH` `PATH-00155` — evidencia: (sin referencias de evidencia)
  - `PATH-00155` → `index/functional_paths.json#PATH-00155`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00155`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00156: proyectos\WebWPF\Formulario156.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario156.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_156` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00156`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_156`).

### Trazabilidad por camino

- `PATH` `PATH-00156` — evidencia: (sin referencias de evidencia)
  - `PATH-00156` → `index/functional_paths.json#PATH-00156`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00156`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00157: proyectos\WebWPF\Formulario157.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario157.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_157` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00157`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_157`).

### Trazabilidad por camino

- `PATH` `PATH-00157` — evidencia: (sin referencias de evidencia)
  - `PATH-00157` → `index/functional_paths.json#PATH-00157`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00157`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00158: proyectos\WebWPF\Formulario158.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario158.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_158` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00158`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_158`).

### Trazabilidad por camino

- `PATH` `PATH-00158` — evidencia: (sin referencias de evidencia)
  - `PATH-00158` → `index/functional_paths.json#PATH-00158`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00158`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00159: proyectos\WebWPF\Formulario159.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario159.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_159` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00159`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_159`).

### Trazabilidad por camino

- `PATH` `PATH-00159` — evidencia: (sin referencias de evidencia)
  - `PATH-00159` → `index/functional_paths.json#PATH-00159`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00159`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00016: proyectos\WebWPF\Formulario16.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario16.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_16` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00016`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_16`).

### Trazabilidad por camino

- `PATH` `PATH-00016` — evidencia: (sin referencias de evidencia)
  - `PATH-00016` → `index/functional_paths.json#PATH-00016`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00016`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00160: proyectos\WebWPF\Formulario160.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario160.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_160` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00160`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_160`).

### Trazabilidad por camino

- `PATH` `PATH-00160` — evidencia: (sin referencias de evidencia)
  - `PATH-00160` → `index/functional_paths.json#PATH-00160`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00160`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00161: proyectos\WebWPF\Formulario161.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario161.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_161` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00161`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_161`).

### Trazabilidad por camino

- `PATH` `PATH-00161` — evidencia: (sin referencias de evidencia)
  - `PATH-00161` → `index/functional_paths.json#PATH-00161`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00161`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00162: proyectos\WebWPF\Formulario162.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario162.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_162` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00162`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_162`).

### Trazabilidad por camino

- `PATH` `PATH-00162` — evidencia: (sin referencias de evidencia)
  - `PATH-00162` → `index/functional_paths.json#PATH-00162`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00162`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00163: proyectos\WebWPF\Formulario163.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario163.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_163` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00163`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_163`).

### Trazabilidad por camino

- `PATH` `PATH-00163` — evidencia: (sin referencias de evidencia)
  - `PATH-00163` → `index/functional_paths.json#PATH-00163`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00163`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00164: proyectos\WebWPF\Formulario164.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario164.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_164` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00164`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_164`).

### Trazabilidad por camino

- `PATH` `PATH-00164` — evidencia: (sin referencias de evidencia)
  - `PATH-00164` → `index/functional_paths.json#PATH-00164`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00164`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00165: proyectos\WebWPF\Formulario165.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario165.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_165` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00165`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_165`).

### Trazabilidad por camino

- `PATH` `PATH-00165` — evidencia: (sin referencias de evidencia)
  - `PATH-00165` → `index/functional_paths.json#PATH-00165`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00165`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00166: proyectos\WebWPF\Formulario166.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario166.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_166` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00166`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_166`).

### Trazabilidad por camino

- `PATH` `PATH-00166` — evidencia: (sin referencias de evidencia)
  - `PATH-00166` → `index/functional_paths.json#PATH-00166`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00166`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00167: proyectos\WebWPF\Formulario167.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario167.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_167` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00167`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_167`).

### Trazabilidad por camino

- `PATH` `PATH-00167` — evidencia: (sin referencias de evidencia)
  - `PATH-00167` → `index/functional_paths.json#PATH-00167`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00167`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00168: proyectos\WebWPF\Formulario168.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario168.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_168` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00168`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_168`).

### Trazabilidad por camino

- `PATH` `PATH-00168` — evidencia: (sin referencias de evidencia)
  - `PATH-00168` → `index/functional_paths.json#PATH-00168`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00168`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00169: proyectos\WebWPF\Formulario169.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario169.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_169` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00169`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_169`).

### Trazabilidad por camino

- `PATH` `PATH-00169` — evidencia: (sin referencias de evidencia)
  - `PATH-00169` → `index/functional_paths.json#PATH-00169`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00169`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00017: proyectos\WebWPF\Formulario17.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario17.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_17` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00017`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_17`).

### Trazabilidad por camino

- `PATH` `PATH-00017` — evidencia: (sin referencias de evidencia)
  - `PATH-00017` → `index/functional_paths.json#PATH-00017`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00017`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00170: proyectos\WebWPF\Formulario170.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario170.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_170` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00170`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_170`).

### Trazabilidad por camino

- `PATH` `PATH-00170` — evidencia: (sin referencias de evidencia)
  - `PATH-00170` → `index/functional_paths.json#PATH-00170`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00170`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00171: proyectos\WebWPF\Formulario171.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario171.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_171` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00171`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_171`).

### Trazabilidad por camino

- `PATH` `PATH-00171` — evidencia: (sin referencias de evidencia)
  - `PATH-00171` → `index/functional_paths.json#PATH-00171`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00171`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00172: proyectos\WebWPF\Formulario172.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario172.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_172` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00172`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_172`).

### Trazabilidad por camino

- `PATH` `PATH-00172` — evidencia: (sin referencias de evidencia)
  - `PATH-00172` → `index/functional_paths.json#PATH-00172`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00172`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00173: proyectos\WebWPF\Formulario173.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario173.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_173` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00173`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_173`).

### Trazabilidad por camino

- `PATH` `PATH-00173` — evidencia: (sin referencias de evidencia)
  - `PATH-00173` → `index/functional_paths.json#PATH-00173`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00173`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00174: proyectos\WebWPF\Formulario174.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario174.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_174` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00174`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_174`).

### Trazabilidad por camino

- `PATH` `PATH-00174` — evidencia: (sin referencias de evidencia)
  - `PATH-00174` → `index/functional_paths.json#PATH-00174`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00174`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00175: proyectos\WebWPF\Formulario175.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario175.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_175` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00175`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_175`).

### Trazabilidad por camino

- `PATH` `PATH-00175` — evidencia: (sin referencias de evidencia)
  - `PATH-00175` → `index/functional_paths.json#PATH-00175`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00175`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00176: proyectos\WebWPF\Formulario176.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario176.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_176` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00176`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_176`).

### Trazabilidad por camino

- `PATH` `PATH-00176` — evidencia: (sin referencias de evidencia)
  - `PATH-00176` → `index/functional_paths.json#PATH-00176`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00176`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00177: proyectos\WebWPF\Formulario177.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario177.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_177` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00177`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_177`).

### Trazabilidad por camino

- `PATH` `PATH-00177` — evidencia: (sin referencias de evidencia)
  - `PATH-00177` → `index/functional_paths.json#PATH-00177`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00177`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00178: proyectos\WebWPF\Formulario178.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario178.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_178` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00178`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_178`).

### Trazabilidad por camino

- `PATH` `PATH-00178` — evidencia: (sin referencias de evidencia)
  - `PATH-00178` → `index/functional_paths.json#PATH-00178`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00178`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00179: proyectos\WebWPF\Formulario179.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario179.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_179` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00179`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_179`).

### Trazabilidad por camino

- `PATH` `PATH-00179` — evidencia: (sin referencias de evidencia)
  - `PATH-00179` → `index/functional_paths.json#PATH-00179`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00179`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00018: proyectos\WebWPF\Formulario18.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario18.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_18` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00018`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_18`).

### Trazabilidad por camino

- `PATH` `PATH-00018` — evidencia: (sin referencias de evidencia)
  - `PATH-00018` → `index/functional_paths.json#PATH-00018`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00018`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00180: proyectos\WebWPF\Formulario180.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario180.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_180` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00180`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_180`).

### Trazabilidad por camino

- `PATH` `PATH-00180` — evidencia: (sin referencias de evidencia)
  - `PATH-00180` → `index/functional_paths.json#PATH-00180`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00180`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00181: proyectos\WebWPF\Formulario181.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario181.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_181` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00181`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_181`).

### Trazabilidad por camino

- `PATH` `PATH-00181` — evidencia: (sin referencias de evidencia)
  - `PATH-00181` → `index/functional_paths.json#PATH-00181`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00181`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00182: proyectos\WebWPF\Formulario182.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario182.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_182` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00182`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_182`).

### Trazabilidad por camino

- `PATH` `PATH-00182` — evidencia: (sin referencias de evidencia)
  - `PATH-00182` → `index/functional_paths.json#PATH-00182`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00182`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00183: proyectos\WebWPF\Formulario183.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario183.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_183` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00183`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_183`).

### Trazabilidad por camino

- `PATH` `PATH-00183` — evidencia: (sin referencias de evidencia)
  - `PATH-00183` → `index/functional_paths.json#PATH-00183`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00183`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00184: proyectos\WebWPF\Formulario184.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario184.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_184` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00184`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_184`).

### Trazabilidad por camino

- `PATH` `PATH-00184` — evidencia: (sin referencias de evidencia)
  - `PATH-00184` → `index/functional_paths.json#PATH-00184`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00184`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00185: proyectos\WebWPF\Formulario185.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario185.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_185` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00185`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_185`).

### Trazabilidad por camino

- `PATH` `PATH-00185` — evidencia: (sin referencias de evidencia)
  - `PATH-00185` → `index/functional_paths.json#PATH-00185`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00185`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00186: proyectos\WebWPF\Formulario186.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario186.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_186` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00186`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_186`).

### Trazabilidad por camino

- `PATH` `PATH-00186` — evidencia: (sin referencias de evidencia)
  - `PATH-00186` → `index/functional_paths.json#PATH-00186`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00186`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00187: proyectos\WebWPF\Formulario187.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario187.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_187` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00187`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_187`).

### Trazabilidad por camino

- `PATH` `PATH-00187` — evidencia: (sin referencias de evidencia)
  - `PATH-00187` → `index/functional_paths.json#PATH-00187`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00187`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00188: proyectos\WebWPF\Formulario188.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario188.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_188` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00188`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_188`).

### Trazabilidad por camino

- `PATH` `PATH-00188` — evidencia: (sin referencias de evidencia)
  - `PATH-00188` → `index/functional_paths.json#PATH-00188`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00188`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00189: proyectos\WebWPF\Formulario189.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario189.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_189` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00189`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_189`).

### Trazabilidad por camino

- `PATH` `PATH-00189` — evidencia: (sin referencias de evidencia)
  - `PATH-00189` → `index/functional_paths.json#PATH-00189`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00189`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00019: proyectos\WebWPF\Formulario19.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario19.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_19` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00019`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_19`).

### Trazabilidad por camino

- `PATH` `PATH-00019` — evidencia: (sin referencias de evidencia)
  - `PATH-00019` → `index/functional_paths.json#PATH-00019`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00019`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00190: proyectos\WebWPF\Formulario190.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario190.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_190` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00190`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_190`).

### Trazabilidad por camino

- `PATH` `PATH-00190` — evidencia: (sin referencias de evidencia)
  - `PATH-00190` → `index/functional_paths.json#PATH-00190`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00190`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00191: proyectos\WebWPF\Formulario191.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario191.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_191` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00191`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_191`).

### Trazabilidad por camino

- `PATH` `PATH-00191` — evidencia: (sin referencias de evidencia)
  - `PATH-00191` → `index/functional_paths.json#PATH-00191`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00191`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00192: proyectos\WebWPF\Formulario192.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario192.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_192` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00192`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_192`).

### Trazabilidad por camino

- `PATH` `PATH-00192` — evidencia: (sin referencias de evidencia)
  - `PATH-00192` → `index/functional_paths.json#PATH-00192`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00192`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00193: proyectos\WebWPF\Formulario193.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario193.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_193` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00193`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_193`).

### Trazabilidad por camino

- `PATH` `PATH-00193` — evidencia: (sin referencias de evidencia)
  - `PATH-00193` → `index/functional_paths.json#PATH-00193`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00193`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00194: proyectos\WebWPF\Formulario194.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario194.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_194` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00194`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_194`).

### Trazabilidad por camino

- `PATH` `PATH-00194` — evidencia: (sin referencias de evidencia)
  - `PATH-00194` → `index/functional_paths.json#PATH-00194`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00194`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00195: proyectos\WebWPF\Formulario195.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario195.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_195` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00195`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_195`).

### Trazabilidad por camino

- `PATH` `PATH-00195` — evidencia: (sin referencias de evidencia)
  - `PATH-00195` → `index/functional_paths.json#PATH-00195`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00195`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00196: proyectos\WebWPF\Formulario196.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario196.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_196` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00196`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_196`).

### Trazabilidad por camino

- `PATH` `PATH-00196` — evidencia: (sin referencias de evidencia)
  - `PATH-00196` → `index/functional_paths.json#PATH-00196`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00196`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00197: proyectos\WebWPF\Formulario197.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario197.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_197` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00197`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_197`).

### Trazabilidad por camino

- `PATH` `PATH-00197` — evidencia: (sin referencias de evidencia)
  - `PATH-00197` → `index/functional_paths.json#PATH-00197`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00197`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00198: proyectos\WebWPF\Formulario198.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario198.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_198` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00198`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_198`).

### Trazabilidad por camino

- `PATH` `PATH-00198` — evidencia: (sin referencias de evidencia)
  - `PATH-00198` → `index/functional_paths.json#PATH-00198`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00198`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00199: proyectos\WebWPF\Formulario199.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario199.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_199` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00199`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_199`).

### Trazabilidad por camino

- `PATH` `PATH-00199` — evidencia: (sin referencias de evidencia)
  - `PATH-00199` → `index/functional_paths.json#PATH-00199`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00199`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00002: proyectos\WebWPF\Formulario2.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario2.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_2` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00002`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_2`).

### Trazabilidad por camino

- `PATH` `PATH-00002` — evidencia: (sin referencias de evidencia)
  - `PATH-00002` → `index/functional_paths.json#PATH-00002`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00002`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00020: proyectos\WebWPF\Formulario20.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario20.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_20` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00020`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_20`).

### Trazabilidad por camino

- `PATH` `PATH-00020` — evidencia: (sin referencias de evidencia)
  - `PATH-00020` → `index/functional_paths.json#PATH-00020`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00020`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00200: proyectos\WebWPF\Formulario200.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario200.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_200` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00200`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_200`).

### Trazabilidad por camino

- `PATH` `PATH-00200` — evidencia: (sin referencias de evidencia)
  - `PATH-00200` → `index/functional_paths.json#PATH-00200`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00200`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00201: proyectos\WebWPF\Formulario201.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario201.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_201` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00201`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_201`).

### Trazabilidad por camino

- `PATH` `PATH-00201` — evidencia: (sin referencias de evidencia)
  - `PATH-00201` → `index/functional_paths.json#PATH-00201`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00201`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00202: proyectos\WebWPF\Formulario202.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario202.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_202` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00202`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_202`).

### Trazabilidad por camino

- `PATH` `PATH-00202` — evidencia: (sin referencias de evidencia)
  - `PATH-00202` → `index/functional_paths.json#PATH-00202`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00202`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00203: proyectos\WebWPF\Formulario203.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario203.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_203` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00203`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_203`).

### Trazabilidad por camino

- `PATH` `PATH-00203` — evidencia: (sin referencias de evidencia)
  - `PATH-00203` → `index/functional_paths.json#PATH-00203`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00203`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00204: proyectos\WebWPF\Formulario204.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario204.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_204` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00204`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_204`).

### Trazabilidad por camino

- `PATH` `PATH-00204` — evidencia: (sin referencias de evidencia)
  - `PATH-00204` → `index/functional_paths.json#PATH-00204`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00204`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00205: proyectos\WebWPF\Formulario205.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario205.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_205` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00205`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_205`).

### Trazabilidad por camino

- `PATH` `PATH-00205` — evidencia: (sin referencias de evidencia)
  - `PATH-00205` → `index/functional_paths.json#PATH-00205`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00205`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00206: proyectos\WebWPF\Formulario206.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario206.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_206` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00206`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_206`).

### Trazabilidad por camino

- `PATH` `PATH-00206` — evidencia: (sin referencias de evidencia)
  - `PATH-00206` → `index/functional_paths.json#PATH-00206`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00206`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00207: proyectos\WebWPF\Formulario207.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario207.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_207` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00207`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_207`).

### Trazabilidad por camino

- `PATH` `PATH-00207` — evidencia: (sin referencias de evidencia)
  - `PATH-00207` → `index/functional_paths.json#PATH-00207`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00207`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00208: proyectos\WebWPF\Formulario208.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario208.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_208` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00208`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_208`).

### Trazabilidad por camino

- `PATH` `PATH-00208` — evidencia: (sin referencias de evidencia)
  - `PATH-00208` → `index/functional_paths.json#PATH-00208`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00208`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00209: proyectos\WebWPF\Formulario209.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario209.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_209` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00209`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_209`).

### Trazabilidad por camino

- `PATH` `PATH-00209` — evidencia: (sin referencias de evidencia)
  - `PATH-00209` → `index/functional_paths.json#PATH-00209`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00209`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00021: proyectos\WebWPF\Formulario21.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario21.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_21` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00021`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_21`).

### Trazabilidad por camino

- `PATH` `PATH-00021` — evidencia: (sin referencias de evidencia)
  - `PATH-00021` → `index/functional_paths.json#PATH-00021`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00021`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00210: proyectos\WebWPF\Formulario210.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario210.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_210` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00210`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_210`).

### Trazabilidad por camino

- `PATH` `PATH-00210` — evidencia: (sin referencias de evidencia)
  - `PATH-00210` → `index/functional_paths.json#PATH-00210`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00210`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00211: proyectos\WebWPF\Formulario211.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario211.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_211` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00211`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_211`).

### Trazabilidad por camino

- `PATH` `PATH-00211` — evidencia: (sin referencias de evidencia)
  - `PATH-00211` → `index/functional_paths.json#PATH-00211`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00211`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00212: proyectos\WebWPF\Formulario212.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario212.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_212` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00212`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_212`).

### Trazabilidad por camino

- `PATH` `PATH-00212` — evidencia: (sin referencias de evidencia)
  - `PATH-00212` → `index/functional_paths.json#PATH-00212`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00212`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00213: proyectos\WebWPF\Formulario213.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario213.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_213` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00213`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_213`).

### Trazabilidad por camino

- `PATH` `PATH-00213` — evidencia: (sin referencias de evidencia)
  - `PATH-00213` → `index/functional_paths.json#PATH-00213`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00213`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00214: proyectos\WebWPF\Formulario214.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario214.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_214` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00214`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_214`).

### Trazabilidad por camino

- `PATH` `PATH-00214` — evidencia: (sin referencias de evidencia)
  - `PATH-00214` → `index/functional_paths.json#PATH-00214`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00214`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00215: proyectos\WebWPF\Formulario215.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario215.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_215` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00215`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_215`).

### Trazabilidad por camino

- `PATH` `PATH-00215` — evidencia: (sin referencias de evidencia)
  - `PATH-00215` → `index/functional_paths.json#PATH-00215`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00215`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00216: proyectos\WebWPF\Formulario216.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario216.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_216` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00216`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_216`).

### Trazabilidad por camino

- `PATH` `PATH-00216` — evidencia: (sin referencias de evidencia)
  - `PATH-00216` → `index/functional_paths.json#PATH-00216`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00216`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00217: proyectos\WebWPF\Formulario217.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario217.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_217` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00217`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_217`).

### Trazabilidad por camino

- `PATH` `PATH-00217` — evidencia: (sin referencias de evidencia)
  - `PATH-00217` → `index/functional_paths.json#PATH-00217`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00217`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00218: proyectos\WebWPF\Formulario218.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario218.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_218` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00218`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_218`).

### Trazabilidad por camino

- `PATH` `PATH-00218` — evidencia: (sin referencias de evidencia)
  - `PATH-00218` → `index/functional_paths.json#PATH-00218`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00218`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00219: proyectos\WebWPF\Formulario219.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario219.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_219` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00219`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_219`).

### Trazabilidad por camino

- `PATH` `PATH-00219` — evidencia: (sin referencias de evidencia)
  - `PATH-00219` → `index/functional_paths.json#PATH-00219`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00219`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00022: proyectos\WebWPF\Formulario22.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario22.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_22` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00022`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_22`).

### Trazabilidad por camino

- `PATH` `PATH-00022` — evidencia: (sin referencias de evidencia)
  - `PATH-00022` → `index/functional_paths.json#PATH-00022`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00022`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00220: proyectos\WebWPF\Formulario220.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario220.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_220` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00220`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_220`).

### Trazabilidad por camino

- `PATH` `PATH-00220` — evidencia: (sin referencias de evidencia)
  - `PATH-00220` → `index/functional_paths.json#PATH-00220`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00220`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00221: proyectos\WebWPF\Formulario221.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario221.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_221` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00221`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_221`).

### Trazabilidad por camino

- `PATH` `PATH-00221` — evidencia: (sin referencias de evidencia)
  - `PATH-00221` → `index/functional_paths.json#PATH-00221`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00221`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00222: proyectos\WebWPF\Formulario222.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario222.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_222` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00222`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_222`).

### Trazabilidad por camino

- `PATH` `PATH-00222` — evidencia: (sin referencias de evidencia)
  - `PATH-00222` → `index/functional_paths.json#PATH-00222`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00222`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00223: proyectos\WebWPF\Formulario223.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario223.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_223` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00223`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_223`).

### Trazabilidad por camino

- `PATH` `PATH-00223` — evidencia: (sin referencias de evidencia)
  - `PATH-00223` → `index/functional_paths.json#PATH-00223`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00223`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00224: proyectos\WebWPF\Formulario224.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario224.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_224` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00224`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_224`).

### Trazabilidad por camino

- `PATH` `PATH-00224` — evidencia: (sin referencias de evidencia)
  - `PATH-00224` → `index/functional_paths.json#PATH-00224`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00224`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00225: proyectos\WebWPF\Formulario225.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario225.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_225` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00225`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_225`).

### Trazabilidad por camino

- `PATH` `PATH-00225` — evidencia: (sin referencias de evidencia)
  - `PATH-00225` → `index/functional_paths.json#PATH-00225`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00225`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00226: proyectos\WebWPF\Formulario226.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario226.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_226` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00226`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_226`).

### Trazabilidad por camino

- `PATH` `PATH-00226` — evidencia: (sin referencias de evidencia)
  - `PATH-00226` → `index/functional_paths.json#PATH-00226`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00226`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00227: proyectos\WebWPF\Formulario227.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario227.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_227` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00227`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_227`).

### Trazabilidad por camino

- `PATH` `PATH-00227` — evidencia: (sin referencias de evidencia)
  - `PATH-00227` → `index/functional_paths.json#PATH-00227`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00227`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00228: proyectos\WebWPF\Formulario228.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario228.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_228` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00228`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_228`).

### Trazabilidad por camino

- `PATH` `PATH-00228` — evidencia: (sin referencias de evidencia)
  - `PATH-00228` → `index/functional_paths.json#PATH-00228`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00228`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00229: proyectos\WebWPF\Formulario229.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario229.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_229` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00229`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_229`).

### Trazabilidad por camino

- `PATH` `PATH-00229` — evidencia: (sin referencias de evidencia)
  - `PATH-00229` → `index/functional_paths.json#PATH-00229`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00229`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00023: proyectos\WebWPF\Formulario23.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario23.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_23` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00023`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_23`).

### Trazabilidad por camino

- `PATH` `PATH-00023` — evidencia: (sin referencias de evidencia)
  - `PATH-00023` → `index/functional_paths.json#PATH-00023`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00023`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00230: proyectos\WebWPF\Formulario230.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario230.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_230` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00230`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_230`).

### Trazabilidad por camino

- `PATH` `PATH-00230` — evidencia: (sin referencias de evidencia)
  - `PATH-00230` → `index/functional_paths.json#PATH-00230`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00230`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00231: proyectos\WebWPF\Formulario231.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario231.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_231` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00231`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_231`).

### Trazabilidad por camino

- `PATH` `PATH-00231` — evidencia: (sin referencias de evidencia)
  - `PATH-00231` → `index/functional_paths.json#PATH-00231`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00231`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00232: proyectos\WebWPF\Formulario232.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario232.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_232` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00232`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_232`).

### Trazabilidad por camino

- `PATH` `PATH-00232` — evidencia: (sin referencias de evidencia)
  - `PATH-00232` → `index/functional_paths.json#PATH-00232`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00232`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00233: proyectos\WebWPF\Formulario233.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario233.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_233` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00233`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_233`).

### Trazabilidad por camino

- `PATH` `PATH-00233` — evidencia: (sin referencias de evidencia)
  - `PATH-00233` → `index/functional_paths.json#PATH-00233`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00233`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00234: proyectos\WebWPF\Formulario234.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario234.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_234` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00234`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_234`).

### Trazabilidad por camino

- `PATH` `PATH-00234` — evidencia: (sin referencias de evidencia)
  - `PATH-00234` → `index/functional_paths.json#PATH-00234`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00234`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00235: proyectos\WebWPF\Formulario235.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario235.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_235` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00235`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_235`).

### Trazabilidad por camino

- `PATH` `PATH-00235` — evidencia: (sin referencias de evidencia)
  - `PATH-00235` → `index/functional_paths.json#PATH-00235`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00235`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00236: proyectos\WebWPF\Formulario236.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario236.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_236` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00236`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_236`).

### Trazabilidad por camino

- `PATH` `PATH-00236` — evidencia: (sin referencias de evidencia)
  - `PATH-00236` → `index/functional_paths.json#PATH-00236`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00236`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00237: proyectos\WebWPF\Formulario237.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario237.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_237` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00237`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_237`).

### Trazabilidad por camino

- `PATH` `PATH-00237` — evidencia: (sin referencias de evidencia)
  - `PATH-00237` → `index/functional_paths.json#PATH-00237`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00237`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00238: proyectos\WebWPF\Formulario238.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario238.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_238` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00238`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_238`).

### Trazabilidad por camino

- `PATH` `PATH-00238` — evidencia: (sin referencias de evidencia)
  - `PATH-00238` → `index/functional_paths.json#PATH-00238`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00238`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00239: proyectos\WebWPF\Formulario239.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario239.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_239` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00239`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_239`).

### Trazabilidad por camino

- `PATH` `PATH-00239` — evidencia: (sin referencias de evidencia)
  - `PATH-00239` → `index/functional_paths.json#PATH-00239`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00239`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00024: proyectos\WebWPF\Formulario24.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario24.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_24` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00024`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_24`).

### Trazabilidad por camino

- `PATH` `PATH-00024` — evidencia: (sin referencias de evidencia)
  - `PATH-00024` → `index/functional_paths.json#PATH-00024`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00024`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00240: proyectos\WebWPF\Formulario240.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario240.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_240` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00240`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_240`).

### Trazabilidad por camino

- `PATH` `PATH-00240` — evidencia: (sin referencias de evidencia)
  - `PATH-00240` → `index/functional_paths.json#PATH-00240`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00240`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00241: proyectos\WebWPF\Formulario241.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario241.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_241` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00241`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_241`).

### Trazabilidad por camino

- `PATH` `PATH-00241` — evidencia: (sin referencias de evidencia)
  - `PATH-00241` → `index/functional_paths.json#PATH-00241`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00241`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00242: proyectos\WebWPF\Formulario242.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario242.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_242` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00242`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_242`).

### Trazabilidad por camino

- `PATH` `PATH-00242` — evidencia: (sin referencias de evidencia)
  - `PATH-00242` → `index/functional_paths.json#PATH-00242`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00242`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00243: proyectos\WebWPF\Formulario243.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario243.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_243` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00243`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_243`).

### Trazabilidad por camino

- `PATH` `PATH-00243` — evidencia: (sin referencias de evidencia)
  - `PATH-00243` → `index/functional_paths.json#PATH-00243`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00243`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00244: proyectos\WebWPF\Formulario244.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario244.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_244` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00244`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_244`).

### Trazabilidad por camino

- `PATH` `PATH-00244` — evidencia: (sin referencias de evidencia)
  - `PATH-00244` → `index/functional_paths.json#PATH-00244`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00244`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00245: proyectos\WebWPF\Formulario245.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario245.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_245` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00245`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_245`).

### Trazabilidad por camino

- `PATH` `PATH-00245` — evidencia: (sin referencias de evidencia)
  - `PATH-00245` → `index/functional_paths.json#PATH-00245`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00245`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00246: proyectos\WebWPF\Formulario246.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario246.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_246` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00246`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_246`).

### Trazabilidad por camino

- `PATH` `PATH-00246` — evidencia: (sin referencias de evidencia)
  - `PATH-00246` → `index/functional_paths.json#PATH-00246`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00246`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00247: proyectos\WebWPF\Formulario247.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario247.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_247` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00247`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_247`).

### Trazabilidad por camino

- `PATH` `PATH-00247` — evidencia: (sin referencias de evidencia)
  - `PATH-00247` → `index/functional_paths.json#PATH-00247`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00247`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00248: proyectos\WebWPF\Formulario248.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario248.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_248` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00248`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_248`).

### Trazabilidad por camino

- `PATH` `PATH-00248` — evidencia: (sin referencias de evidencia)
  - `PATH-00248` → `index/functional_paths.json#PATH-00248`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00248`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00249: proyectos\WebWPF\Formulario249.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario249.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_249` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00249`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_249`).

### Trazabilidad por camino

- `PATH` `PATH-00249` — evidencia: (sin referencias de evidencia)
  - `PATH-00249` → `index/functional_paths.json#PATH-00249`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00249`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00025: proyectos\WebWPF\Formulario25.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario25.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_25` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00025`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_25`).

### Trazabilidad por camino

- `PATH` `PATH-00025` — evidencia: (sin referencias de evidencia)
  - `PATH-00025` → `index/functional_paths.json#PATH-00025`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00025`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00250: proyectos\WebWPF\Formulario250.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario250.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_250` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00250`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_250`).

### Trazabilidad por camino

- `PATH` `PATH-00250` — evidencia: (sin referencias de evidencia)
  - `PATH-00250` → `index/functional_paths.json#PATH-00250`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00250`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00251: proyectos\WebWPF\Formulario251.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario251.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_251` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00251`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_251`).

### Trazabilidad por camino

- `PATH` `PATH-00251` — evidencia: (sin referencias de evidencia)
  - `PATH-00251` → `index/functional_paths.json#PATH-00251`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00251`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00252: proyectos\WebWPF\Formulario252.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario252.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_252` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00252`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_252`).

### Trazabilidad por camino

- `PATH` `PATH-00252` — evidencia: (sin referencias de evidencia)
  - `PATH-00252` → `index/functional_paths.json#PATH-00252`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00252`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00253: proyectos\WebWPF\Formulario253.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario253.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_253` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00253`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_253`).

### Trazabilidad por camino

- `PATH` `PATH-00253` — evidencia: (sin referencias de evidencia)
  - `PATH-00253` → `index/functional_paths.json#PATH-00253`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00253`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00254: proyectos\WebWPF\Formulario254.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario254.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_254` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00254`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_254`).

### Trazabilidad por camino

- `PATH` `PATH-00254` — evidencia: (sin referencias de evidencia)
  - `PATH-00254` → `index/functional_paths.json#PATH-00254`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00254`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00255: proyectos\WebWPF\Formulario255.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario255.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_255` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00255`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_255`).

### Trazabilidad por camino

- `PATH` `PATH-00255` — evidencia: (sin referencias de evidencia)
  - `PATH-00255` → `index/functional_paths.json#PATH-00255`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00255`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00256: proyectos\WebWPF\Formulario256.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario256.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_256` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00256`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_256`).

### Trazabilidad por camino

- `PATH` `PATH-00256` — evidencia: (sin referencias de evidencia)
  - `PATH-00256` → `index/functional_paths.json#PATH-00256`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00256`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00257: proyectos\WebWPF\Formulario257.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario257.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_257` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00257`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_257`).

### Trazabilidad por camino

- `PATH` `PATH-00257` — evidencia: (sin referencias de evidencia)
  - `PATH-00257` → `index/functional_paths.json#PATH-00257`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00257`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00258: proyectos\WebWPF\Formulario258.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario258.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_258` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00258`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_258`).

### Trazabilidad por camino

- `PATH` `PATH-00258` — evidencia: (sin referencias de evidencia)
  - `PATH-00258` → `index/functional_paths.json#PATH-00258`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00258`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00259: proyectos\WebWPF\Formulario259.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario259.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_259` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00259`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_259`).

### Trazabilidad por camino

- `PATH` `PATH-00259` — evidencia: (sin referencias de evidencia)
  - `PATH-00259` → `index/functional_paths.json#PATH-00259`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00259`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00026: proyectos\WebWPF\Formulario26.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario26.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_26` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00026`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_26`).

### Trazabilidad por camino

- `PATH` `PATH-00026` — evidencia: (sin referencias de evidencia)
  - `PATH-00026` → `index/functional_paths.json#PATH-00026`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00026`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00260: proyectos\WebWPF\Formulario260.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario260.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_260` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00260`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_260`).

### Trazabilidad por camino

- `PATH` `PATH-00260` — evidencia: (sin referencias de evidencia)
  - `PATH-00260` → `index/functional_paths.json#PATH-00260`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00260`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00261: proyectos\WebWPF\Formulario261.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario261.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_261` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00261`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_261`).

### Trazabilidad por camino

- `PATH` `PATH-00261` — evidencia: (sin referencias de evidencia)
  - `PATH-00261` → `index/functional_paths.json#PATH-00261`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00261`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00262: proyectos\WebWPF\Formulario262.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario262.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_262` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00262`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_262`).

### Trazabilidad por camino

- `PATH` `PATH-00262` — evidencia: (sin referencias de evidencia)
  - `PATH-00262` → `index/functional_paths.json#PATH-00262`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00262`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00263: proyectos\WebWPF\Formulario263.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario263.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_263` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00263`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_263`).

### Trazabilidad por camino

- `PATH` `PATH-00263` — evidencia: (sin referencias de evidencia)
  - `PATH-00263` → `index/functional_paths.json#PATH-00263`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00263`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00264: proyectos\WebWPF\Formulario264.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario264.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_264` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00264`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_264`).

### Trazabilidad por camino

- `PATH` `PATH-00264` — evidencia: (sin referencias de evidencia)
  - `PATH-00264` → `index/functional_paths.json#PATH-00264`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00264`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00265: proyectos\WebWPF\Formulario265.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario265.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_265` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00265`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_265`).

### Trazabilidad por camino

- `PATH` `PATH-00265` — evidencia: (sin referencias de evidencia)
  - `PATH-00265` → `index/functional_paths.json#PATH-00265`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00265`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00266: proyectos\WebWPF\Formulario266.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario266.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_266` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00266`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_266`).

### Trazabilidad por camino

- `PATH` `PATH-00266` — evidencia: (sin referencias de evidencia)
  - `PATH-00266` → `index/functional_paths.json#PATH-00266`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00266`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00267: proyectos\WebWPF\Formulario267.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario267.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_267` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00267`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_267`).

### Trazabilidad por camino

- `PATH` `PATH-00267` — evidencia: (sin referencias de evidencia)
  - `PATH-00267` → `index/functional_paths.json#PATH-00267`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00267`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00268: proyectos\WebWPF\Formulario268.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario268.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_268` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00268`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_268`).

### Trazabilidad por camino

- `PATH` `PATH-00268` — evidencia: (sin referencias de evidencia)
  - `PATH-00268` → `index/functional_paths.json#PATH-00268`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00268`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00269: proyectos\WebWPF\Formulario269.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario269.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_269` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00269`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_269`).

### Trazabilidad por camino

- `PATH` `PATH-00269` — evidencia: (sin referencias de evidencia)
  - `PATH-00269` → `index/functional_paths.json#PATH-00269`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00269`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00027: proyectos\WebWPF\Formulario27.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario27.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_27` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00027`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_27`).

### Trazabilidad por camino

- `PATH` `PATH-00027` — evidencia: (sin referencias de evidencia)
  - `PATH-00027` → `index/functional_paths.json#PATH-00027`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00027`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00270: proyectos\WebWPF\Formulario270.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario270.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_270` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00270`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_270`).

### Trazabilidad por camino

- `PATH` `PATH-00270` — evidencia: (sin referencias de evidencia)
  - `PATH-00270` → `index/functional_paths.json#PATH-00270`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00270`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00271: proyectos\WebWPF\Formulario271.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario271.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_271` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00271`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_271`).

### Trazabilidad por camino

- `PATH` `PATH-00271` — evidencia: (sin referencias de evidencia)
  - `PATH-00271` → `index/functional_paths.json#PATH-00271`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00271`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00272: proyectos\WebWPF\Formulario272.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario272.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_272` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00272`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_272`).

### Trazabilidad por camino

- `PATH` `PATH-00272` — evidencia: (sin referencias de evidencia)
  - `PATH-00272` → `index/functional_paths.json#PATH-00272`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00272`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00273: proyectos\WebWPF\Formulario273.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario273.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_273` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00273`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_273`).

### Trazabilidad por camino

- `PATH` `PATH-00273` — evidencia: (sin referencias de evidencia)
  - `PATH-00273` → `index/functional_paths.json#PATH-00273`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00273`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00274: proyectos\WebWPF\Formulario274.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario274.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_274` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00274`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_274`).

### Trazabilidad por camino

- `PATH` `PATH-00274` — evidencia: (sin referencias de evidencia)
  - `PATH-00274` → `index/functional_paths.json#PATH-00274`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00274`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00275: proyectos\WebWPF\Formulario275.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario275.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_275` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00275`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_275`).

### Trazabilidad por camino

- `PATH` `PATH-00275` — evidencia: (sin referencias de evidencia)
  - `PATH-00275` → `index/functional_paths.json#PATH-00275`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00275`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00276: proyectos\WebWPF\Formulario276.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario276.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_276` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00276`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_276`).

### Trazabilidad por camino

- `PATH` `PATH-00276` — evidencia: (sin referencias de evidencia)
  - `PATH-00276` → `index/functional_paths.json#PATH-00276`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00276`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00277: proyectos\WebWPF\Formulario277.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario277.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_277` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00277`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_277`).

### Trazabilidad por camino

- `PATH` `PATH-00277` — evidencia: (sin referencias de evidencia)
  - `PATH-00277` → `index/functional_paths.json#PATH-00277`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00277`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00278: proyectos\WebWPF\Formulario278.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario278.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_278` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00278`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_278`).

### Trazabilidad por camino

- `PATH` `PATH-00278` — evidencia: (sin referencias de evidencia)
  - `PATH-00278` → `index/functional_paths.json#PATH-00278`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00278`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00279: proyectos\WebWPF\Formulario279.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario279.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_279` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00279`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_279`).

### Trazabilidad por camino

- `PATH` `PATH-00279` — evidencia: (sin referencias de evidencia)
  - `PATH-00279` → `index/functional_paths.json#PATH-00279`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00279`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00028: proyectos\WebWPF\Formulario28.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario28.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_28` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00028`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_28`).

### Trazabilidad por camino

- `PATH` `PATH-00028` — evidencia: (sin referencias de evidencia)
  - `PATH-00028` → `index/functional_paths.json#PATH-00028`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00028`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00280: proyectos\WebWPF\Formulario280.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario280.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_280` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00280`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_280`).

### Trazabilidad por camino

- `PATH` `PATH-00280` — evidencia: (sin referencias de evidencia)
  - `PATH-00280` → `index/functional_paths.json#PATH-00280`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00280`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00281: proyectos\WebWPF\Formulario281.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario281.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_281` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00281`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_281`).

### Trazabilidad por camino

- `PATH` `PATH-00281` — evidencia: (sin referencias de evidencia)
  - `PATH-00281` → `index/functional_paths.json#PATH-00281`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00281`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00282: proyectos\WebWPF\Formulario282.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario282.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_282` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00282`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_282`).

### Trazabilidad por camino

- `PATH` `PATH-00282` — evidencia: (sin referencias de evidencia)
  - `PATH-00282` → `index/functional_paths.json#PATH-00282`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00282`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00283: proyectos\WebWPF\Formulario283.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario283.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_283` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00283`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_283`).

### Trazabilidad por camino

- `PATH` `PATH-00283` — evidencia: (sin referencias de evidencia)
  - `PATH-00283` → `index/functional_paths.json#PATH-00283`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00283`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00284: proyectos\WebWPF\Formulario284.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario284.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_284` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00284`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_284`).

### Trazabilidad por camino

- `PATH` `PATH-00284` — evidencia: (sin referencias de evidencia)
  - `PATH-00284` → `index/functional_paths.json#PATH-00284`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00284`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00285: proyectos\WebWPF\Formulario285.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario285.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_285` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00285`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_285`).

### Trazabilidad por camino

- `PATH` `PATH-00285` — evidencia: (sin referencias de evidencia)
  - `PATH-00285` → `index/functional_paths.json#PATH-00285`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00285`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00286: proyectos\WebWPF\Formulario286.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario286.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_286` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00286`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_286`).

### Trazabilidad por camino

- `PATH` `PATH-00286` — evidencia: (sin referencias de evidencia)
  - `PATH-00286` → `index/functional_paths.json#PATH-00286`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00286`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00287: proyectos\WebWPF\Formulario287.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario287.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_287` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00287`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_287`).

### Trazabilidad por camino

- `PATH` `PATH-00287` — evidencia: (sin referencias de evidencia)
  - `PATH-00287` → `index/functional_paths.json#PATH-00287`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00287`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00288: proyectos\WebWPF\Formulario288.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario288.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_288` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00288`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_288`).

### Trazabilidad por camino

- `PATH` `PATH-00288` — evidencia: (sin referencias de evidencia)
  - `PATH-00288` → `index/functional_paths.json#PATH-00288`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00288`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00289: proyectos\WebWPF\Formulario289.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario289.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_289` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00289`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_289`).

### Trazabilidad por camino

- `PATH` `PATH-00289` — evidencia: (sin referencias de evidencia)
  - `PATH-00289` → `index/functional_paths.json#PATH-00289`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00289`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00029: proyectos\WebWPF\Formulario29.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario29.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_29` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00029`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_29`).

### Trazabilidad por camino

- `PATH` `PATH-00029` — evidencia: (sin referencias de evidencia)
  - `PATH-00029` → `index/functional_paths.json#PATH-00029`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00029`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00290: proyectos\WebWPF\Formulario290.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario290.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_290` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00290`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_290`).

### Trazabilidad por camino

- `PATH` `PATH-00290` — evidencia: (sin referencias de evidencia)
  - `PATH-00290` → `index/functional_paths.json#PATH-00290`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00290`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00291: proyectos\WebWPF\Formulario291.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario291.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_291` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00291`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_291`).

### Trazabilidad por camino

- `PATH` `PATH-00291` — evidencia: (sin referencias de evidencia)
  - `PATH-00291` → `index/functional_paths.json#PATH-00291`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00291`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00292: proyectos\WebWPF\Formulario292.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario292.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_292` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00292`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_292`).

### Trazabilidad por camino

- `PATH` `PATH-00292` — evidencia: (sin referencias de evidencia)
  - `PATH-00292` → `index/functional_paths.json#PATH-00292`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00292`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00293: proyectos\WebWPF\Formulario293.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario293.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_293` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00293`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_293`).

### Trazabilidad por camino

- `PATH` `PATH-00293` — evidencia: (sin referencias de evidencia)
  - `PATH-00293` → `index/functional_paths.json#PATH-00293`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00293`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00294: proyectos\WebWPF\Formulario294.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario294.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_294` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00294`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_294`).

### Trazabilidad por camino

- `PATH` `PATH-00294` — evidencia: (sin referencias de evidencia)
  - `PATH-00294` → `index/functional_paths.json#PATH-00294`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00294`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00295: proyectos\WebWPF\Formulario295.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario295.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_295` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00295`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_295`).

### Trazabilidad por camino

- `PATH` `PATH-00295` — evidencia: (sin referencias de evidencia)
  - `PATH-00295` → `index/functional_paths.json#PATH-00295`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00295`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00296: proyectos\WebWPF\Formulario296.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario296.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_296` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00296`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_296`).

### Trazabilidad por camino

- `PATH` `PATH-00296` — evidencia: (sin referencias de evidencia)
  - `PATH-00296` → `index/functional_paths.json#PATH-00296`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00296`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00297: proyectos\WebWPF\Formulario297.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario297.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_297` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00297`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_297`).

### Trazabilidad por camino

- `PATH` `PATH-00297` — evidencia: (sin referencias de evidencia)
  - `PATH-00297` → `index/functional_paths.json#PATH-00297`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00297`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00298: proyectos\WebWPF\Formulario298.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario298.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_298` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00298`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_298`).

### Trazabilidad por camino

- `PATH` `PATH-00298` — evidencia: (sin referencias de evidencia)
  - `PATH-00298` → `index/functional_paths.json#PATH-00298`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00298`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00299: proyectos\WebWPF\Formulario299.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario299.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_299` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00299`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_299`).

### Trazabilidad por camino

- `PATH` `PATH-00299` — evidencia: (sin referencias de evidencia)
  - `PATH-00299` → `index/functional_paths.json#PATH-00299`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00299`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00003: proyectos\WebWPF\Formulario3.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario3.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_3` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00003`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_3`).

### Trazabilidad por camino

- `PATH` `PATH-00003` — evidencia: (sin referencias de evidencia)
  - `PATH-00003` → `index/functional_paths.json#PATH-00003`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00003`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00030: proyectos\WebWPF\Formulario30.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario30.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_30` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00030`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_30`).

### Trazabilidad por camino

- `PATH` `PATH-00030` — evidencia: (sin referencias de evidencia)
  - `PATH-00030` → `index/functional_paths.json#PATH-00030`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00030`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00300: proyectos\WebWPF\Formulario300.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario300.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_300` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00300`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_300`).

### Trazabilidad por camino

- `PATH` `PATH-00300` — evidencia: (sin referencias de evidencia)
  - `PATH-00300` → `index/functional_paths.json#PATH-00300`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00300`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00301: proyectos\WebWPF\Formulario301.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario301.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_301` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00301`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_301`).

### Trazabilidad por camino

- `PATH` `PATH-00301` — evidencia: (sin referencias de evidencia)
  - `PATH-00301` → `index/functional_paths.json#PATH-00301`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00301`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00302: proyectos\WebWPF\Formulario302.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario302.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_302` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00302`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_302`).

### Trazabilidad por camino

- `PATH` `PATH-00302` — evidencia: (sin referencias de evidencia)
  - `PATH-00302` → `index/functional_paths.json#PATH-00302`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00302`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00303: proyectos\WebWPF\Formulario303.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario303.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_303` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00303`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_303`).

### Trazabilidad por camino

- `PATH` `PATH-00303` — evidencia: (sin referencias de evidencia)
  - `PATH-00303` → `index/functional_paths.json#PATH-00303`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00303`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00304: proyectos\WebWPF\Formulario304.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario304.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_304` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00304`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_304`).

### Trazabilidad por camino

- `PATH` `PATH-00304` — evidencia: (sin referencias de evidencia)
  - `PATH-00304` → `index/functional_paths.json#PATH-00304`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00304`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00305: proyectos\WebWPF\Formulario305.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario305.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_305` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00305`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_305`).

### Trazabilidad por camino

- `PATH` `PATH-00305` — evidencia: (sin referencias de evidencia)
  - `PATH-00305` → `index/functional_paths.json#PATH-00305`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00305`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00306: proyectos\WebWPF\Formulario306.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario306.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_306` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00306`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_306`).

### Trazabilidad por camino

- `PATH` `PATH-00306` — evidencia: (sin referencias de evidencia)
  - `PATH-00306` → `index/functional_paths.json#PATH-00306`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00306`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00307: proyectos\WebWPF\Formulario307.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario307.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_307` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00307`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_307`).

### Trazabilidad por camino

- `PATH` `PATH-00307` — evidencia: (sin referencias de evidencia)
  - `PATH-00307` → `index/functional_paths.json#PATH-00307`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00307`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00308: proyectos\WebWPF\Formulario308.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario308.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_308` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00308`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_308`).

### Trazabilidad por camino

- `PATH` `PATH-00308` — evidencia: (sin referencias de evidencia)
  - `PATH-00308` → `index/functional_paths.json#PATH-00308`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00308`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00309: proyectos\WebWPF\Formulario309.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario309.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_309` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00309`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_309`).

### Trazabilidad por camino

- `PATH` `PATH-00309` — evidencia: (sin referencias de evidencia)
  - `PATH-00309` → `index/functional_paths.json#PATH-00309`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00309`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00031: proyectos\WebWPF\Formulario31.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario31.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_31` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00031`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_31`).

### Trazabilidad por camino

- `PATH` `PATH-00031` — evidencia: (sin referencias de evidencia)
  - `PATH-00031` → `index/functional_paths.json#PATH-00031`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00031`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00310: proyectos\WebWPF\Formulario310.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario310.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_310` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00310`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_310`).

### Trazabilidad por camino

- `PATH` `PATH-00310` — evidencia: (sin referencias de evidencia)
  - `PATH-00310` → `index/functional_paths.json#PATH-00310`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00310`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00311: proyectos\WebWPF\Formulario311.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario311.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_311` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00311`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_311`).

### Trazabilidad por camino

- `PATH` `PATH-00311` — evidencia: (sin referencias de evidencia)
  - `PATH-00311` → `index/functional_paths.json#PATH-00311`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00311`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00312: proyectos\WebWPF\Formulario312.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario312.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_312` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00312`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_312`).

### Trazabilidad por camino

- `PATH` `PATH-00312` — evidencia: (sin referencias de evidencia)
  - `PATH-00312` → `index/functional_paths.json#PATH-00312`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00312`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00313: proyectos\WebWPF\Formulario313.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario313.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_313` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00313`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_313`).

### Trazabilidad por camino

- `PATH` `PATH-00313` — evidencia: (sin referencias de evidencia)
  - `PATH-00313` → `index/functional_paths.json#PATH-00313`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00313`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00314: proyectos\WebWPF\Formulario314.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario314.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_314` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00314`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_314`).

### Trazabilidad por camino

- `PATH` `PATH-00314` — evidencia: (sin referencias de evidencia)
  - `PATH-00314` → `index/functional_paths.json#PATH-00314`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00314`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00315: proyectos\WebWPF\Formulario315.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario315.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_315` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00315`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_315`).

### Trazabilidad por camino

- `PATH` `PATH-00315` — evidencia: (sin referencias de evidencia)
  - `PATH-00315` → `index/functional_paths.json#PATH-00315`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00315`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00316: proyectos\WebWPF\Formulario316.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario316.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_316` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00316`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_316`).

### Trazabilidad por camino

- `PATH` `PATH-00316` — evidencia: (sin referencias de evidencia)
  - `PATH-00316` → `index/functional_paths.json#PATH-00316`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00316`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00317: proyectos\WebWPF\Formulario317.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario317.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_317` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00317`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_317`).

### Trazabilidad por camino

- `PATH` `PATH-00317` — evidencia: (sin referencias de evidencia)
  - `PATH-00317` → `index/functional_paths.json#PATH-00317`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00317`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00318: proyectos\WebWPF\Formulario318.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario318.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_318` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00318`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_318`).

### Trazabilidad por camino

- `PATH` `PATH-00318` — evidencia: (sin referencias de evidencia)
  - `PATH-00318` → `index/functional_paths.json#PATH-00318`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00318`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00319: proyectos\WebWPF\Formulario319.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario319.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_319` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00319`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_319`).

### Trazabilidad por camino

- `PATH` `PATH-00319` — evidencia: (sin referencias de evidencia)
  - `PATH-00319` → `index/functional_paths.json#PATH-00319`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00319`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00032: proyectos\WebWPF\Formulario32.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario32.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_32` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00032`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_32`).

### Trazabilidad por camino

- `PATH` `PATH-00032` — evidencia: (sin referencias de evidencia)
  - `PATH-00032` → `index/functional_paths.json#PATH-00032`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00032`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00320: proyectos\WebWPF\Formulario320.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario320.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_320` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00320`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_320`).

### Trazabilidad por camino

- `PATH` `PATH-00320` — evidencia: (sin referencias de evidencia)
  - `PATH-00320` → `index/functional_paths.json#PATH-00320`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00320`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00321: proyectos\WebWPF\Formulario321.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario321.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_321` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00321`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_321`).

### Trazabilidad por camino

- `PATH` `PATH-00321` — evidencia: (sin referencias de evidencia)
  - `PATH-00321` → `index/functional_paths.json#PATH-00321`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00321`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00322: proyectos\WebWPF\Formulario322.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario322.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_322` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00322`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_322`).

### Trazabilidad por camino

- `PATH` `PATH-00322` — evidencia: (sin referencias de evidencia)
  - `PATH-00322` → `index/functional_paths.json#PATH-00322`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00322`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00323: proyectos\WebWPF\Formulario323.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario323.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_323` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00323`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_323`).

### Trazabilidad por camino

- `PATH` `PATH-00323` — evidencia: (sin referencias de evidencia)
  - `PATH-00323` → `index/functional_paths.json#PATH-00323`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00323`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00324: proyectos\WebWPF\Formulario324.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario324.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_324` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00324`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_324`).

### Trazabilidad por camino

- `PATH` `PATH-00324` — evidencia: (sin referencias de evidencia)
  - `PATH-00324` → `index/functional_paths.json#PATH-00324`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00324`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00325: proyectos\WebWPF\Formulario325.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario325.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_325` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00325`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_325`).

### Trazabilidad por camino

- `PATH` `PATH-00325` — evidencia: (sin referencias de evidencia)
  - `PATH-00325` → `index/functional_paths.json#PATH-00325`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00325`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00326: proyectos\WebWPF\Formulario326.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario326.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_326` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00326`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_326`).

### Trazabilidad por camino

- `PATH` `PATH-00326` — evidencia: (sin referencias de evidencia)
  - `PATH-00326` → `index/functional_paths.json#PATH-00326`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00326`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00327: proyectos\WebWPF\Formulario327.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario327.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_327` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00327`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_327`).

### Trazabilidad por camino

- `PATH` `PATH-00327` — evidencia: (sin referencias de evidencia)
  - `PATH-00327` → `index/functional_paths.json#PATH-00327`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00327`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00328: proyectos\WebWPF\Formulario328.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario328.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_328` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00328`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_328`).

### Trazabilidad por camino

- `PATH` `PATH-00328` — evidencia: (sin referencias de evidencia)
  - `PATH-00328` → `index/functional_paths.json#PATH-00328`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00328`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00329: proyectos\WebWPF\Formulario329.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario329.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_329` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00329`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_329`).

### Trazabilidad por camino

- `PATH` `PATH-00329` — evidencia: (sin referencias de evidencia)
  - `PATH-00329` → `index/functional_paths.json#PATH-00329`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00329`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00033: proyectos\WebWPF\Formulario33.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario33.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_33` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00033`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_33`).

### Trazabilidad por camino

- `PATH` `PATH-00033` — evidencia: (sin referencias de evidencia)
  - `PATH-00033` → `index/functional_paths.json#PATH-00033`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00033`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00330: proyectos\WebWPF\Formulario330.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario330.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_330` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00330`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_330`).

### Trazabilidad por camino

- `PATH` `PATH-00330` — evidencia: (sin referencias de evidencia)
  - `PATH-00330` → `index/functional_paths.json#PATH-00330`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00330`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00331: proyectos\WebWPF\Formulario331.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario331.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_331` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00331`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_331`).

### Trazabilidad por camino

- `PATH` `PATH-00331` — evidencia: (sin referencias de evidencia)
  - `PATH-00331` → `index/functional_paths.json#PATH-00331`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00331`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00332: proyectos\WebWPF\Formulario332.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario332.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_332` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00332`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_332`).

### Trazabilidad por camino

- `PATH` `PATH-00332` — evidencia: (sin referencias de evidencia)
  - `PATH-00332` → `index/functional_paths.json#PATH-00332`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00332`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00333: proyectos\WebWPF\Formulario333.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario333.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_333` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00333`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_333`).

### Trazabilidad por camino

- `PATH` `PATH-00333` — evidencia: (sin referencias de evidencia)
  - `PATH-00333` → `index/functional_paths.json#PATH-00333`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00333`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00334: proyectos\WebWPF\Formulario334.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario334.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_334` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00334`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_334`).

### Trazabilidad por camino

- `PATH` `PATH-00334` — evidencia: (sin referencias de evidencia)
  - `PATH-00334` → `index/functional_paths.json#PATH-00334`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00334`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00335: proyectos\WebWPF\Formulario335.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario335.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_335` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00335`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_335`).

### Trazabilidad por camino

- `PATH` `PATH-00335` — evidencia: (sin referencias de evidencia)
  - `PATH-00335` → `index/functional_paths.json#PATH-00335`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00335`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00336: proyectos\WebWPF\Formulario336.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario336.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_336` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00336`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_336`).

### Trazabilidad por camino

- `PATH` `PATH-00336` — evidencia: (sin referencias de evidencia)
  - `PATH-00336` → `index/functional_paths.json#PATH-00336`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00336`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00337: proyectos\WebWPF\Formulario337.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario337.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_337` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00337`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_337`).

### Trazabilidad por camino

- `PATH` `PATH-00337` — evidencia: (sin referencias de evidencia)
  - `PATH-00337` → `index/functional_paths.json#PATH-00337`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00337`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00338: proyectos\WebWPF\Formulario338.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario338.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_338` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00338`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_338`).

### Trazabilidad por camino

- `PATH` `PATH-00338` — evidencia: (sin referencias de evidencia)
  - `PATH-00338` → `index/functional_paths.json#PATH-00338`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00338`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00339: proyectos\WebWPF\Formulario339.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario339.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_339` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00339`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_339`).

### Trazabilidad por camino

- `PATH` `PATH-00339` — evidencia: (sin referencias de evidencia)
  - `PATH-00339` → `index/functional_paths.json#PATH-00339`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00339`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00034: proyectos\WebWPF\Formulario34.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario34.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_34` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00034`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_34`).

### Trazabilidad por camino

- `PATH` `PATH-00034` — evidencia: (sin referencias de evidencia)
  - `PATH-00034` → `index/functional_paths.json#PATH-00034`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00034`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00340: proyectos\WebWPF\Formulario340.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario340.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_340` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00340`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_340`).

### Trazabilidad por camino

- `PATH` `PATH-00340` — evidencia: (sin referencias de evidencia)
  - `PATH-00340` → `index/functional_paths.json#PATH-00340`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00340`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00341: proyectos\WebWPF\Formulario341.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario341.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_341` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00341`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_341`).

### Trazabilidad por camino

- `PATH` `PATH-00341` — evidencia: (sin referencias de evidencia)
  - `PATH-00341` → `index/functional_paths.json#PATH-00341`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00341`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00342: proyectos\WebWPF\Formulario342.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario342.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_342` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00342`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_342`).

### Trazabilidad por camino

- `PATH` `PATH-00342` — evidencia: (sin referencias de evidencia)
  - `PATH-00342` → `index/functional_paths.json#PATH-00342`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00342`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00343: proyectos\WebWPF\Formulario343.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario343.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_343` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00343`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_343`).

### Trazabilidad por camino

- `PATH` `PATH-00343` — evidencia: (sin referencias de evidencia)
  - `PATH-00343` → `index/functional_paths.json#PATH-00343`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00343`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00344: proyectos\WebWPF\Formulario344.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario344.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_344` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00344`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_344`).

### Trazabilidad por camino

- `PATH` `PATH-00344` — evidencia: (sin referencias de evidencia)
  - `PATH-00344` → `index/functional_paths.json#PATH-00344`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00344`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00345: proyectos\WebWPF\Formulario345.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario345.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_345` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00345`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_345`).

### Trazabilidad por camino

- `PATH` `PATH-00345` — evidencia: (sin referencias de evidencia)
  - `PATH-00345` → `index/functional_paths.json#PATH-00345`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00345`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00346: proyectos\WebWPF\Formulario346.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario346.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_346` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00346`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_346`).

### Trazabilidad por camino

- `PATH` `PATH-00346` — evidencia: (sin referencias de evidencia)
  - `PATH-00346` → `index/functional_paths.json#PATH-00346`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00346`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00347: proyectos\WebWPF\Formulario347.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario347.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_347` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00347`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_347`).

### Trazabilidad por camino

- `PATH` `PATH-00347` — evidencia: (sin referencias de evidencia)
  - `PATH-00347` → `index/functional_paths.json#PATH-00347`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00347`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00348: proyectos\WebWPF\Formulario348.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario348.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_348` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00348`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_348`).

### Trazabilidad por camino

- `PATH` `PATH-00348` — evidencia: (sin referencias de evidencia)
  - `PATH-00348` → `index/functional_paths.json#PATH-00348`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00348`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00349: proyectos\WebWPF\Formulario349.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario349.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_349` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00349`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_349`).

### Trazabilidad por camino

- `PATH` `PATH-00349` — evidencia: (sin referencias de evidencia)
  - `PATH-00349` → `index/functional_paths.json#PATH-00349`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00349`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00035: proyectos\WebWPF\Formulario35.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario35.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_35` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00035`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_35`).

### Trazabilidad por camino

- `PATH` `PATH-00035` — evidencia: (sin referencias de evidencia)
  - `PATH-00035` → `index/functional_paths.json#PATH-00035`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00035`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00350: proyectos\WebWPF\Formulario350.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario350.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_350` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00350`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_350`).

### Trazabilidad por camino

- `PATH` `PATH-00350` — evidencia: (sin referencias de evidencia)
  - `PATH-00350` → `index/functional_paths.json#PATH-00350`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00350`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00351: proyectos\WebWPF\Formulario351.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario351.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_351` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00351`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_351`).

### Trazabilidad por camino

- `PATH` `PATH-00351` — evidencia: (sin referencias de evidencia)
  - `PATH-00351` → `index/functional_paths.json#PATH-00351`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00351`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00352: proyectos\WebWPF\Formulario352.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario352.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_352` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00352`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_352`).

### Trazabilidad por camino

- `PATH` `PATH-00352` — evidencia: (sin referencias de evidencia)
  - `PATH-00352` → `index/functional_paths.json#PATH-00352`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00352`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00353: proyectos\WebWPF\Formulario353.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario353.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_353` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00353`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_353`).

### Trazabilidad por camino

- `PATH` `PATH-00353` — evidencia: (sin referencias de evidencia)
  - `PATH-00353` → `index/functional_paths.json#PATH-00353`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00353`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00354: proyectos\WebWPF\Formulario354.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario354.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_354` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00354`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_354`).

### Trazabilidad por camino

- `PATH` `PATH-00354` — evidencia: (sin referencias de evidencia)
  - `PATH-00354` → `index/functional_paths.json#PATH-00354`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00354`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00355: proyectos\WebWPF\Formulario355.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario355.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_355` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00355`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_355`).

### Trazabilidad por camino

- `PATH` `PATH-00355` — evidencia: (sin referencias de evidencia)
  - `PATH-00355` → `index/functional_paths.json#PATH-00355`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00355`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00356: proyectos\WebWPF\Formulario356.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario356.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_356` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00356`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_356`).

### Trazabilidad por camino

- `PATH` `PATH-00356` — evidencia: (sin referencias de evidencia)
  - `PATH-00356` → `index/functional_paths.json#PATH-00356`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00356`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00357: proyectos\WebWPF\Formulario357.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario357.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_357` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00357`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_357`).

### Trazabilidad por camino

- `PATH` `PATH-00357` — evidencia: (sin referencias de evidencia)
  - `PATH-00357` → `index/functional_paths.json#PATH-00357`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00357`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00358: proyectos\WebWPF\Formulario358.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario358.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_358` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00358`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_358`).

### Trazabilidad por camino

- `PATH` `PATH-00358` — evidencia: (sin referencias de evidencia)
  - `PATH-00358` → `index/functional_paths.json#PATH-00358`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00358`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00359: proyectos\WebWPF\Formulario359.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario359.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_359` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00359`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_359`).

### Trazabilidad por camino

- `PATH` `PATH-00359` — evidencia: (sin referencias de evidencia)
  - `PATH-00359` → `index/functional_paths.json#PATH-00359`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00359`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00036: proyectos\WebWPF\Formulario36.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario36.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_36` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00036`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_36`).

### Trazabilidad por camino

- `PATH` `PATH-00036` — evidencia: (sin referencias de evidencia)
  - `PATH-00036` → `index/functional_paths.json#PATH-00036`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00036`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00360: proyectos\WebWPF\Formulario360.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario360.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_360` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00360`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_360`).

### Trazabilidad por camino

- `PATH` `PATH-00360` — evidencia: (sin referencias de evidencia)
  - `PATH-00360` → `index/functional_paths.json#PATH-00360`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00360`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00361: proyectos\WebWPF\Formulario361.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario361.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_361` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00361`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_361`).

### Trazabilidad por camino

- `PATH` `PATH-00361` — evidencia: (sin referencias de evidencia)
  - `PATH-00361` → `index/functional_paths.json#PATH-00361`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00361`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00362: proyectos\WebWPF\Formulario362.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario362.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_362` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00362`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_362`).

### Trazabilidad por camino

- `PATH` `PATH-00362` — evidencia: (sin referencias de evidencia)
  - `PATH-00362` → `index/functional_paths.json#PATH-00362`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00362`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00363: proyectos\WebWPF\Formulario363.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario363.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_363` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00363`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_363`).

### Trazabilidad por camino

- `PATH` `PATH-00363` — evidencia: (sin referencias de evidencia)
  - `PATH-00363` → `index/functional_paths.json#PATH-00363`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00363`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00364: proyectos\WebWPF\Formulario364.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario364.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_364` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00364`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_364`).

### Trazabilidad por camino

- `PATH` `PATH-00364` — evidencia: (sin referencias de evidencia)
  - `PATH-00364` → `index/functional_paths.json#PATH-00364`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00364`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00365: proyectos\WebWPF\Formulario365.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario365.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_365` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00365`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_365`).

### Trazabilidad por camino

- `PATH` `PATH-00365` — evidencia: (sin referencias de evidencia)
  - `PATH-00365` → `index/functional_paths.json#PATH-00365`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00365`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00366: proyectos\WebWPF\Formulario366.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario366.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_366` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00366`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_366`).

### Trazabilidad por camino

- `PATH` `PATH-00366` — evidencia: (sin referencias de evidencia)
  - `PATH-00366` → `index/functional_paths.json#PATH-00366`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00366`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00367: proyectos\WebWPF\Formulario367.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario367.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_367` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00367`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_367`).

### Trazabilidad por camino

- `PATH` `PATH-00367` — evidencia: (sin referencias de evidencia)
  - `PATH-00367` → `index/functional_paths.json#PATH-00367`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00367`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00368: proyectos\WebWPF\Formulario368.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario368.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_368` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00368`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_368`).

### Trazabilidad por camino

- `PATH` `PATH-00368` — evidencia: (sin referencias de evidencia)
  - `PATH-00368` → `index/functional_paths.json#PATH-00368`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00368`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00369: proyectos\WebWPF\Formulario369.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario369.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_369` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00369`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_369`).

### Trazabilidad por camino

- `PATH` `PATH-00369` — evidencia: (sin referencias de evidencia)
  - `PATH-00369` → `index/functional_paths.json#PATH-00369`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00369`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00037: proyectos\WebWPF\Formulario37.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario37.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_37` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00037`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_37`).

### Trazabilidad por camino

- `PATH` `PATH-00037` — evidencia: (sin referencias de evidencia)
  - `PATH-00037` → `index/functional_paths.json#PATH-00037`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00037`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00370: proyectos\WebWPF\Formulario370.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario370.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_370` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00370`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_370`).

### Trazabilidad por camino

- `PATH` `PATH-00370` — evidencia: (sin referencias de evidencia)
  - `PATH-00370` → `index/functional_paths.json#PATH-00370`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00370`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00371: proyectos\WebWPF\Formulario371.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario371.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_371` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00371`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_371`).

### Trazabilidad por camino

- `PATH` `PATH-00371` — evidencia: (sin referencias de evidencia)
  - `PATH-00371` → `index/functional_paths.json#PATH-00371`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00371`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00372: proyectos\WebWPF\Formulario372.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario372.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_372` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00372`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_372`).

### Trazabilidad por camino

- `PATH` `PATH-00372` — evidencia: (sin referencias de evidencia)
  - `PATH-00372` → `index/functional_paths.json#PATH-00372`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00372`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00373: proyectos\WebWPF\Formulario373.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario373.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_373` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00373`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_373`).

### Trazabilidad por camino

- `PATH` `PATH-00373` — evidencia: (sin referencias de evidencia)
  - `PATH-00373` → `index/functional_paths.json#PATH-00373`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00373`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00374: proyectos\WebWPF\Formulario374.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario374.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_374` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00374`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_374`).

### Trazabilidad por camino

- `PATH` `PATH-00374` — evidencia: (sin referencias de evidencia)
  - `PATH-00374` → `index/functional_paths.json#PATH-00374`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00374`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00375: proyectos\WebWPF\Formulario375.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario375.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_375` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00375`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_375`).

### Trazabilidad por camino

- `PATH` `PATH-00375` — evidencia: (sin referencias de evidencia)
  - `PATH-00375` → `index/functional_paths.json#PATH-00375`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00375`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00376: proyectos\WebWPF\Formulario376.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario376.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_376` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00376`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_376`).

### Trazabilidad por camino

- `PATH` `PATH-00376` — evidencia: (sin referencias de evidencia)
  - `PATH-00376` → `index/functional_paths.json#PATH-00376`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00376`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00377: proyectos\WebWPF\Formulario377.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario377.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_377` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00377`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_377`).

### Trazabilidad por camino

- `PATH` `PATH-00377` — evidencia: (sin referencias de evidencia)
  - `PATH-00377` → `index/functional_paths.json#PATH-00377`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00377`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00378: proyectos\WebWPF\Formulario378.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario378.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_378` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00378`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_378`).

### Trazabilidad por camino

- `PATH` `PATH-00378` — evidencia: (sin referencias de evidencia)
  - `PATH-00378` → `index/functional_paths.json#PATH-00378`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00378`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00379: proyectos\WebWPF\Formulario379.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario379.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_379` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00379`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_379`).

### Trazabilidad por camino

- `PATH` `PATH-00379` — evidencia: (sin referencias de evidencia)
  - `PATH-00379` → `index/functional_paths.json#PATH-00379`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00379`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00038: proyectos\WebWPF\Formulario38.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario38.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_38` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00038`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_38`).

### Trazabilidad por camino

- `PATH` `PATH-00038` — evidencia: (sin referencias de evidencia)
  - `PATH-00038` → `index/functional_paths.json#PATH-00038`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00038`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00380: proyectos\WebWPF\Formulario380.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario380.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_380` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00380`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_380`).

### Trazabilidad por camino

- `PATH` `PATH-00380` — evidencia: (sin referencias de evidencia)
  - `PATH-00380` → `index/functional_paths.json#PATH-00380`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00380`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00381: proyectos\WebWPF\Formulario381.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario381.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_381` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00381`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_381`).

### Trazabilidad por camino

- `PATH` `PATH-00381` — evidencia: (sin referencias de evidencia)
  - `PATH-00381` → `index/functional_paths.json#PATH-00381`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00381`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00382: proyectos\WebWPF\Formulario382.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario382.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_382` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00382`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_382`).

### Trazabilidad por camino

- `PATH` `PATH-00382` — evidencia: (sin referencias de evidencia)
  - `PATH-00382` → `index/functional_paths.json#PATH-00382`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00382`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00383: proyectos\WebWPF\Formulario383.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario383.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_383` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00383`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_383`).

### Trazabilidad por camino

- `PATH` `PATH-00383` — evidencia: (sin referencias de evidencia)
  - `PATH-00383` → `index/functional_paths.json#PATH-00383`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00383`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00384: proyectos\WebWPF\Formulario384.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario384.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_384` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00384`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_384`).

### Trazabilidad por camino

- `PATH` `PATH-00384` — evidencia: (sin referencias de evidencia)
  - `PATH-00384` → `index/functional_paths.json#PATH-00384`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00384`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00385: proyectos\WebWPF\Formulario385.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario385.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_385` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00385`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_385`).

### Trazabilidad por camino

- `PATH` `PATH-00385` — evidencia: (sin referencias de evidencia)
  - `PATH-00385` → `index/functional_paths.json#PATH-00385`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00385`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00386: proyectos\WebWPF\Formulario386.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario386.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_386` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00386`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_386`).

### Trazabilidad por camino

- `PATH` `PATH-00386` — evidencia: (sin referencias de evidencia)
  - `PATH-00386` → `index/functional_paths.json#PATH-00386`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00386`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00387: proyectos\WebWPF\Formulario387.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario387.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_387` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00387`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_387`).

### Trazabilidad por camino

- `PATH` `PATH-00387` — evidencia: (sin referencias de evidencia)
  - `PATH-00387` → `index/functional_paths.json#PATH-00387`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00387`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00388: proyectos\WebWPF\Formulario388.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario388.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_388` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00388`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_388`).

### Trazabilidad por camino

- `PATH` `PATH-00388` — evidencia: (sin referencias de evidencia)
  - `PATH-00388` → `index/functional_paths.json#PATH-00388`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00388`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00389: proyectos\WebWPF\Formulario389.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario389.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_389` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00389`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_389`).

### Trazabilidad por camino

- `PATH` `PATH-00389` — evidencia: (sin referencias de evidencia)
  - `PATH-00389` → `index/functional_paths.json#PATH-00389`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00389`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00039: proyectos\WebWPF\Formulario39.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario39.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_39` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00039`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_39`).

### Trazabilidad por camino

- `PATH` `PATH-00039` — evidencia: (sin referencias de evidencia)
  - `PATH-00039` → `index/functional_paths.json#PATH-00039`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00039`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00390: proyectos\WebWPF\Formulario390.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario390.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_390` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00390`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_390`).

### Trazabilidad por camino

- `PATH` `PATH-00390` — evidencia: (sin referencias de evidencia)
  - `PATH-00390` → `index/functional_paths.json#PATH-00390`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00390`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00391: proyectos\WebWPF\Formulario391.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario391.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_391` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00391`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_391`).

### Trazabilidad por camino

- `PATH` `PATH-00391` — evidencia: (sin referencias de evidencia)
  - `PATH-00391` → `index/functional_paths.json#PATH-00391`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00391`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00392: proyectos\WebWPF\Formulario392.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario392.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_392` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00392`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_392`).

### Trazabilidad por camino

- `PATH` `PATH-00392` — evidencia: (sin referencias de evidencia)
  - `PATH-00392` → `index/functional_paths.json#PATH-00392`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00392`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00393: proyectos\WebWPF\Formulario393.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario393.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_393` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00393`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_393`).

### Trazabilidad por camino

- `PATH` `PATH-00393` — evidencia: (sin referencias de evidencia)
  - `PATH-00393` → `index/functional_paths.json#PATH-00393`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00393`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00394: proyectos\WebWPF\Formulario394.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario394.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_394` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00394`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_394`).

### Trazabilidad por camino

- `PATH` `PATH-00394` — evidencia: (sin referencias de evidencia)
  - `PATH-00394` → `index/functional_paths.json#PATH-00394`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00394`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00395: proyectos\WebWPF\Formulario395.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario395.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_395` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00395`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_395`).

### Trazabilidad por camino

- `PATH` `PATH-00395` — evidencia: (sin referencias de evidencia)
  - `PATH-00395` → `index/functional_paths.json#PATH-00395`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00395`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00396: proyectos\WebWPF\Formulario396.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario396.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_396` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00396`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_396`).

### Trazabilidad por camino

- `PATH` `PATH-00396` — evidencia: (sin referencias de evidencia)
  - `PATH-00396` → `index/functional_paths.json#PATH-00396`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00396`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00397: proyectos\WebWPF\Formulario397.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario397.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_397` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00397`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_397`).

### Trazabilidad por camino

- `PATH` `PATH-00397` — evidencia: (sin referencias de evidencia)
  - `PATH-00397` → `index/functional_paths.json#PATH-00397`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00397`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00398: proyectos\WebWPF\Formulario398.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario398.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_398` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00398`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_398`).

### Trazabilidad por camino

- `PATH` `PATH-00398` — evidencia: (sin referencias de evidencia)
  - `PATH-00398` → `index/functional_paths.json#PATH-00398`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00398`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00399: proyectos\WebWPF\Formulario399.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario399.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_399` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00399`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_399`).

### Trazabilidad por camino

- `PATH` `PATH-00399` — evidencia: (sin referencias de evidencia)
  - `PATH-00399` → `index/functional_paths.json#PATH-00399`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00399`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00004: proyectos\WebWPF\Formulario4.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario4.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_4` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00004`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_4`).

### Trazabilidad por camino

- `PATH` `PATH-00004` — evidencia: (sin referencias de evidencia)
  - `PATH-00004` → `index/functional_paths.json#PATH-00004`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00004`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00040: proyectos\WebWPF\Formulario40.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario40.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_40` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00040`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_40`).

### Trazabilidad por camino

- `PATH` `PATH-00040` — evidencia: (sin referencias de evidencia)
  - `PATH-00040` → `index/functional_paths.json#PATH-00040`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00040`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00400: proyectos\WebWPF\Formulario400.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario400.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_400` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00400`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_400`).

### Trazabilidad por camino

- `PATH` `PATH-00400` — evidencia: (sin referencias de evidencia)
  - `PATH-00400` → `index/functional_paths.json#PATH-00400`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00400`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00401: proyectos\WebWPF\Formulario401.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario401.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_401` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00401`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_401`).

### Trazabilidad por camino

- `PATH` `PATH-00401` — evidencia: (sin referencias de evidencia)
  - `PATH-00401` → `index/functional_paths.json#PATH-00401`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00401`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00402: proyectos\WebWPF\Formulario402.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario402.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_402` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00402`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_402`).

### Trazabilidad por camino

- `PATH` `PATH-00402` — evidencia: (sin referencias de evidencia)
  - `PATH-00402` → `index/functional_paths.json#PATH-00402`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00402`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00403: proyectos\WebWPF\Formulario403.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario403.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_403` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00403`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_403`).

### Trazabilidad por camino

- `PATH` `PATH-00403` — evidencia: (sin referencias de evidencia)
  - `PATH-00403` → `index/functional_paths.json#PATH-00403`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00403`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00404: proyectos\WebWPF\Formulario404.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario404.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_404` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00404`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_404`).

### Trazabilidad por camino

- `PATH` `PATH-00404` — evidencia: (sin referencias de evidencia)
  - `PATH-00404` → `index/functional_paths.json#PATH-00404`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00404`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00405: proyectos\WebWPF\Formulario405.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario405.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_405` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00405`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_405`).

### Trazabilidad por camino

- `PATH` `PATH-00405` — evidencia: (sin referencias de evidencia)
  - `PATH-00405` → `index/functional_paths.json#PATH-00405`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00405`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00406: proyectos\WebWPF\Formulario406.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario406.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_406` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00406`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_406`).

### Trazabilidad por camino

- `PATH` `PATH-00406` — evidencia: (sin referencias de evidencia)
  - `PATH-00406` → `index/functional_paths.json#PATH-00406`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00406`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00407: proyectos\WebWPF\Formulario407.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario407.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_407` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00407`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_407`).

### Trazabilidad por camino

- `PATH` `PATH-00407` — evidencia: (sin referencias de evidencia)
  - `PATH-00407` → `index/functional_paths.json#PATH-00407`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00407`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00408: proyectos\WebWPF\Formulario408.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario408.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_408` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00408`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_408`).

### Trazabilidad por camino

- `PATH` `PATH-00408` — evidencia: (sin referencias de evidencia)
  - `PATH-00408` → `index/functional_paths.json#PATH-00408`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00408`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00409: proyectos\WebWPF\Formulario409.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario409.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_409` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00409`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_409`).

### Trazabilidad por camino

- `PATH` `PATH-00409` — evidencia: (sin referencias de evidencia)
  - `PATH-00409` → `index/functional_paths.json#PATH-00409`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00409`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00041: proyectos\WebWPF\Formulario41.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario41.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_41` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00041`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_41`).

### Trazabilidad por camino

- `PATH` `PATH-00041` — evidencia: (sin referencias de evidencia)
  - `PATH-00041` → `index/functional_paths.json#PATH-00041`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00041`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00410: proyectos\WebWPF\Formulario410.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario410.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_410` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00410`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_410`).

### Trazabilidad por camino

- `PATH` `PATH-00410` — evidencia: (sin referencias de evidencia)
  - `PATH-00410` → `index/functional_paths.json#PATH-00410`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00410`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00411: proyectos\WebWPF\Formulario411.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario411.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_411` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00411`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_411`).

### Trazabilidad por camino

- `PATH` `PATH-00411` — evidencia: (sin referencias de evidencia)
  - `PATH-00411` → `index/functional_paths.json#PATH-00411`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00411`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00412: proyectos\WebWPF\Formulario412.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario412.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_412` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00412`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_412`).

### Trazabilidad por camino

- `PATH` `PATH-00412` — evidencia: (sin referencias de evidencia)
  - `PATH-00412` → `index/functional_paths.json#PATH-00412`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00412`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00413: proyectos\WebWPF\Formulario413.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario413.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_413` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00413`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_413`).

### Trazabilidad por camino

- `PATH` `PATH-00413` — evidencia: (sin referencias de evidencia)
  - `PATH-00413` → `index/functional_paths.json#PATH-00413`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00413`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00414: proyectos\WebWPF\Formulario414.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario414.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_414` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00414`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_414`).

### Trazabilidad por camino

- `PATH` `PATH-00414` — evidencia: (sin referencias de evidencia)
  - `PATH-00414` → `index/functional_paths.json#PATH-00414`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00414`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00415: proyectos\WebWPF\Formulario415.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario415.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_415` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00415`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_415`).

### Trazabilidad por camino

- `PATH` `PATH-00415` — evidencia: (sin referencias de evidencia)
  - `PATH-00415` → `index/functional_paths.json#PATH-00415`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00415`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00416: proyectos\WebWPF\Formulario416.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario416.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_416` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00416`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_416`).

### Trazabilidad por camino

- `PATH` `PATH-00416` — evidencia: (sin referencias de evidencia)
  - `PATH-00416` → `index/functional_paths.json#PATH-00416`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00416`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00417: proyectos\WebWPF\Formulario417.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario417.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_417` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00417`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_417`).

### Trazabilidad por camino

- `PATH` `PATH-00417` — evidencia: (sin referencias de evidencia)
  - `PATH-00417` → `index/functional_paths.json#PATH-00417`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00417`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00418: proyectos\WebWPF\Formulario418.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario418.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_418` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00418`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_418`).

### Trazabilidad por camino

- `PATH` `PATH-00418` — evidencia: (sin referencias de evidencia)
  - `PATH-00418` → `index/functional_paths.json#PATH-00418`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00418`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00419: proyectos\WebWPF\Formulario419.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario419.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_419` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00419`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_419`).

### Trazabilidad por camino

- `PATH` `PATH-00419` — evidencia: (sin referencias de evidencia)
  - `PATH-00419` → `index/functional_paths.json#PATH-00419`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00419`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00042: proyectos\WebWPF\Formulario42.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario42.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_42` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00042`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_42`).

### Trazabilidad por camino

- `PATH` `PATH-00042` — evidencia: (sin referencias de evidencia)
  - `PATH-00042` → `index/functional_paths.json#PATH-00042`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00042`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00420: proyectos\WebWPF\Formulario420.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario420.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_420` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00420`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_420`).

### Trazabilidad por camino

- `PATH` `PATH-00420` — evidencia: (sin referencias de evidencia)
  - `PATH-00420` → `index/functional_paths.json#PATH-00420`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00420`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00421: proyectos\WebWPF\Formulario421.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario421.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_421` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00421`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_421`).

### Trazabilidad por camino

- `PATH` `PATH-00421` — evidencia: (sin referencias de evidencia)
  - `PATH-00421` → `index/functional_paths.json#PATH-00421`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00421`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00422: proyectos\WebWPF\Formulario422.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario422.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_422` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00422`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_422`).

### Trazabilidad por camino

- `PATH` `PATH-00422` — evidencia: (sin referencias de evidencia)
  - `PATH-00422` → `index/functional_paths.json#PATH-00422`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00422`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00423: proyectos\WebWPF\Formulario423.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario423.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_423` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00423`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_423`).

### Trazabilidad por camino

- `PATH` `PATH-00423` — evidencia: (sin referencias de evidencia)
  - `PATH-00423` → `index/functional_paths.json#PATH-00423`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00423`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00424: proyectos\WebWPF\Formulario424.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario424.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_424` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00424`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_424`).

### Trazabilidad por camino

- `PATH` `PATH-00424` — evidencia: (sin referencias de evidencia)
  - `PATH-00424` → `index/functional_paths.json#PATH-00424`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00424`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00425: proyectos\WebWPF\Formulario425.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario425.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_425` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00425`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_425`).

### Trazabilidad por camino

- `PATH` `PATH-00425` — evidencia: (sin referencias de evidencia)
  - `PATH-00425` → `index/functional_paths.json#PATH-00425`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00425`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00426: proyectos\WebWPF\Formulario426.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario426.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_426` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00426`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_426`).

### Trazabilidad por camino

- `PATH` `PATH-00426` — evidencia: (sin referencias de evidencia)
  - `PATH-00426` → `index/functional_paths.json#PATH-00426`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00426`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00427: proyectos\WebWPF\Formulario427.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario427.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_427` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00427`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_427`).

### Trazabilidad por camino

- `PATH` `PATH-00427` — evidencia: (sin referencias de evidencia)
  - `PATH-00427` → `index/functional_paths.json#PATH-00427`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00427`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00428: proyectos\WebWPF\Formulario428.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario428.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_428` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00428`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_428`).

### Trazabilidad por camino

- `PATH` `PATH-00428` — evidencia: (sin referencias de evidencia)
  - `PATH-00428` → `index/functional_paths.json#PATH-00428`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00428`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00429: proyectos\WebWPF\Formulario429.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario429.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_429` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00429`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_429`).

### Trazabilidad por camino

- `PATH` `PATH-00429` — evidencia: (sin referencias de evidencia)
  - `PATH-00429` → `index/functional_paths.json#PATH-00429`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00429`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00043: proyectos\WebWPF\Formulario43.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario43.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_43` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00043`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_43`).

### Trazabilidad por camino

- `PATH` `PATH-00043` — evidencia: (sin referencias de evidencia)
  - `PATH-00043` → `index/functional_paths.json#PATH-00043`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00043`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00430: proyectos\WebWPF\Formulario430.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario430.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_430` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00430`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_430`).

### Trazabilidad por camino

- `PATH` `PATH-00430` — evidencia: (sin referencias de evidencia)
  - `PATH-00430` → `index/functional_paths.json#PATH-00430`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00430`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00431: proyectos\WebWPF\Formulario431.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario431.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_431` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00431`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_431`).

### Trazabilidad por camino

- `PATH` `PATH-00431` — evidencia: (sin referencias de evidencia)
  - `PATH-00431` → `index/functional_paths.json#PATH-00431`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00431`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00432: proyectos\WebWPF\Formulario432.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario432.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_432` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00432`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_432`).

### Trazabilidad por camino

- `PATH` `PATH-00432` — evidencia: (sin referencias de evidencia)
  - `PATH-00432` → `index/functional_paths.json#PATH-00432`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00432`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00433: proyectos\WebWPF\Formulario433.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario433.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_433` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00433`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_433`).

### Trazabilidad por camino

- `PATH` `PATH-00433` — evidencia: (sin referencias de evidencia)
  - `PATH-00433` → `index/functional_paths.json#PATH-00433`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00433`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00434: proyectos\WebWPF\Formulario434.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario434.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_434` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00434`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_434`).

### Trazabilidad por camino

- `PATH` `PATH-00434` — evidencia: (sin referencias de evidencia)
  - `PATH-00434` → `index/functional_paths.json#PATH-00434`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00434`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00435: proyectos\WebWPF\Formulario435.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario435.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_435` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00435`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_435`).

### Trazabilidad por camino

- `PATH` `PATH-00435` — evidencia: (sin referencias de evidencia)
  - `PATH-00435` → `index/functional_paths.json#PATH-00435`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00435`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00436: proyectos\WebWPF\Formulario436.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario436.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_436` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00436`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_436`).

### Trazabilidad por camino

- `PATH` `PATH-00436` — evidencia: (sin referencias de evidencia)
  - `PATH-00436` → `index/functional_paths.json#PATH-00436`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00436`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00437: proyectos\WebWPF\Formulario437.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario437.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_437` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00437`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_437`).

### Trazabilidad por camino

- `PATH` `PATH-00437` — evidencia: (sin referencias de evidencia)
  - `PATH-00437` → `index/functional_paths.json#PATH-00437`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00437`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00438: proyectos\WebWPF\Formulario438.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario438.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_438` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00438`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_438`).

### Trazabilidad por camino

- `PATH` `PATH-00438` — evidencia: (sin referencias de evidencia)
  - `PATH-00438` → `index/functional_paths.json#PATH-00438`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00438`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00439: proyectos\WebWPF\Formulario439.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario439.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_439` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00439`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_439`).

### Trazabilidad por camino

- `PATH` `PATH-00439` — evidencia: (sin referencias de evidencia)
  - `PATH-00439` → `index/functional_paths.json#PATH-00439`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00439`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00044: proyectos\WebWPF\Formulario44.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario44.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_44` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00044`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_44`).

### Trazabilidad por camino

- `PATH` `PATH-00044` — evidencia: (sin referencias de evidencia)
  - `PATH-00044` → `index/functional_paths.json#PATH-00044`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00044`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00440: proyectos\WebWPF\Formulario440.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario440.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_440` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00440`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_440`).

### Trazabilidad por camino

- `PATH` `PATH-00440` — evidencia: (sin referencias de evidencia)
  - `PATH-00440` → `index/functional_paths.json#PATH-00440`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00440`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00441: proyectos\WebWPF\Formulario441.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario441.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_441` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00441`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_441`).

### Trazabilidad por camino

- `PATH` `PATH-00441` — evidencia: (sin referencias de evidencia)
  - `PATH-00441` → `index/functional_paths.json#PATH-00441`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00441`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00442: proyectos\WebWPF\Formulario442.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario442.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_442` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00442`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_442`).

### Trazabilidad por camino

- `PATH` `PATH-00442` — evidencia: (sin referencias de evidencia)
  - `PATH-00442` → `index/functional_paths.json#PATH-00442`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00442`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00443: proyectos\WebWPF\Formulario443.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario443.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_443` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00443`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_443`).

### Trazabilidad por camino

- `PATH` `PATH-00443` — evidencia: (sin referencias de evidencia)
  - `PATH-00443` → `index/functional_paths.json#PATH-00443`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00443`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00444: proyectos\WebWPF\Formulario444.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario444.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_444` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00444`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_444`).

### Trazabilidad por camino

- `PATH` `PATH-00444` — evidencia: (sin referencias de evidencia)
  - `PATH-00444` → `index/functional_paths.json#PATH-00444`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00444`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00445: proyectos\WebWPF\Formulario445.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario445.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_445` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00445`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_445`).

### Trazabilidad por camino

- `PATH` `PATH-00445` — evidencia: (sin referencias de evidencia)
  - `PATH-00445` → `index/functional_paths.json#PATH-00445`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00445`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00446: proyectos\WebWPF\Formulario446.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario446.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_446` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00446`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_446`).

### Trazabilidad por camino

- `PATH` `PATH-00446` — evidencia: (sin referencias de evidencia)
  - `PATH-00446` → `index/functional_paths.json#PATH-00446`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00446`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00447: proyectos\WebWPF\Formulario447.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario447.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_447` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00447`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_447`).

### Trazabilidad por camino

- `PATH` `PATH-00447` — evidencia: (sin referencias de evidencia)
  - `PATH-00447` → `index/functional_paths.json#PATH-00447`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00447`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00448: proyectos\WebWPF\Formulario448.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario448.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_448` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00448`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_448`).

### Trazabilidad por camino

- `PATH` `PATH-00448` — evidencia: (sin referencias de evidencia)
  - `PATH-00448` → `index/functional_paths.json#PATH-00448`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00448`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00449: proyectos\WebWPF\Formulario449.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario449.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_449` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00449`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_449`).

### Trazabilidad por camino

- `PATH` `PATH-00449` — evidencia: (sin referencias de evidencia)
  - `PATH-00449` → `index/functional_paths.json#PATH-00449`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00449`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00045: proyectos\WebWPF\Formulario45.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario45.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_45` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00045`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_45`).

### Trazabilidad por camino

- `PATH` `PATH-00045` — evidencia: (sin referencias de evidencia)
  - `PATH-00045` → `index/functional_paths.json#PATH-00045`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00045`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00450: proyectos\WebWPF\Formulario450.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario450.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_450` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00450`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_450`).

### Trazabilidad por camino

- `PATH` `PATH-00450` — evidencia: (sin referencias de evidencia)
  - `PATH-00450` → `index/functional_paths.json#PATH-00450`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00450`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00451: proyectos\WebWPF\Formulario451.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario451.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_451` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00451`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_451`).

### Trazabilidad por camino

- `PATH` `PATH-00451` — evidencia: (sin referencias de evidencia)
  - `PATH-00451` → `index/functional_paths.json#PATH-00451`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00451`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00452: proyectos\WebWPF\Formulario452.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario452.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_452` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00452`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_452`).

### Trazabilidad por camino

- `PATH` `PATH-00452` — evidencia: (sin referencias de evidencia)
  - `PATH-00452` → `index/functional_paths.json#PATH-00452`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00452`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00453: proyectos\WebWPF\Formulario453.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario453.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_453` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00453`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_453`).

### Trazabilidad por camino

- `PATH` `PATH-00453` — evidencia: (sin referencias de evidencia)
  - `PATH-00453` → `index/functional_paths.json#PATH-00453`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00453`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00454: proyectos\WebWPF\Formulario454.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario454.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_454` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00454`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_454`).

### Trazabilidad por camino

- `PATH` `PATH-00454` — evidencia: (sin referencias de evidencia)
  - `PATH-00454` → `index/functional_paths.json#PATH-00454`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00454`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00455: proyectos\WebWPF\Formulario455.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario455.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_455` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00455`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_455`).

### Trazabilidad por camino

- `PATH` `PATH-00455` — evidencia: (sin referencias de evidencia)
  - `PATH-00455` → `index/functional_paths.json#PATH-00455`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00455`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00456: proyectos\WebWPF\Formulario456.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario456.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_456` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00456`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_456`).

### Trazabilidad por camino

- `PATH` `PATH-00456` — evidencia: (sin referencias de evidencia)
  - `PATH-00456` → `index/functional_paths.json#PATH-00456`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00456`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00457: proyectos\WebWPF\Formulario457.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario457.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_457` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00457`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_457`).

### Trazabilidad por camino

- `PATH` `PATH-00457` — evidencia: (sin referencias de evidencia)
  - `PATH-00457` → `index/functional_paths.json#PATH-00457`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00457`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00458: proyectos\WebWPF\Formulario458.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario458.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_458` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00458`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_458`).

### Trazabilidad por camino

- `PATH` `PATH-00458` — evidencia: (sin referencias de evidencia)
  - `PATH-00458` → `index/functional_paths.json#PATH-00458`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00458`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00459: proyectos\WebWPF\Formulario459.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario459.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_459` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00459`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_459`).

### Trazabilidad por camino

- `PATH` `PATH-00459` — evidencia: (sin referencias de evidencia)
  - `PATH-00459` → `index/functional_paths.json#PATH-00459`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00459`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00046: proyectos\WebWPF\Formulario46.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario46.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_46` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00046`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_46`).

### Trazabilidad por camino

- `PATH` `PATH-00046` — evidencia: (sin referencias de evidencia)
  - `PATH-00046` → `index/functional_paths.json#PATH-00046`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00046`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00460: proyectos\WebWPF\Formulario460.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario460.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_460` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00460`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_460`).

### Trazabilidad por camino

- `PATH` `PATH-00460` — evidencia: (sin referencias de evidencia)
  - `PATH-00460` → `index/functional_paths.json#PATH-00460`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00460`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00461: proyectos\WebWPF\Formulario461.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario461.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_461` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00461`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_461`).

### Trazabilidad por camino

- `PATH` `PATH-00461` — evidencia: (sin referencias de evidencia)
  - `PATH-00461` → `index/functional_paths.json#PATH-00461`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00461`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00462: proyectos\WebWPF\Formulario462.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario462.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_462` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00462`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_462`).

### Trazabilidad por camino

- `PATH` `PATH-00462` — evidencia: (sin referencias de evidencia)
  - `PATH-00462` → `index/functional_paths.json#PATH-00462`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00462`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00463: proyectos\WebWPF\Formulario463.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario463.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_463` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00463`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_463`).

### Trazabilidad por camino

- `PATH` `PATH-00463` — evidencia: (sin referencias de evidencia)
  - `PATH-00463` → `index/functional_paths.json#PATH-00463`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00463`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00464: proyectos\WebWPF\Formulario464.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario464.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_464` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00464`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_464`).

### Trazabilidad por camino

- `PATH` `PATH-00464` — evidencia: (sin referencias de evidencia)
  - `PATH-00464` → `index/functional_paths.json#PATH-00464`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00464`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00465: proyectos\WebWPF\Formulario465.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario465.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_465` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00465`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_465`).

### Trazabilidad por camino

- `PATH` `PATH-00465` — evidencia: (sin referencias de evidencia)
  - `PATH-00465` → `index/functional_paths.json#PATH-00465`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00465`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00466: proyectos\WebWPF\Formulario466.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario466.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_466` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00466`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_466`).

### Trazabilidad por camino

- `PATH` `PATH-00466` — evidencia: (sin referencias de evidencia)
  - `PATH-00466` → `index/functional_paths.json#PATH-00466`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00466`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00467: proyectos\WebWPF\Formulario467.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario467.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_467` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00467`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_467`).

### Trazabilidad por camino

- `PATH` `PATH-00467` — evidencia: (sin referencias de evidencia)
  - `PATH-00467` → `index/functional_paths.json#PATH-00467`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00467`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00468: proyectos\WebWPF\Formulario468.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario468.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_468` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00468`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_468`).

### Trazabilidad por camino

- `PATH` `PATH-00468` — evidencia: (sin referencias de evidencia)
  - `PATH-00468` → `index/functional_paths.json#PATH-00468`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00468`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00469: proyectos\WebWPF\Formulario469.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario469.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_469` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00469`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_469`).

### Trazabilidad por camino

- `PATH` `PATH-00469` — evidencia: (sin referencias de evidencia)
  - `PATH-00469` → `index/functional_paths.json#PATH-00469`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00469`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00047: proyectos\WebWPF\Formulario47.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario47.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_47` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00047`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_47`).

### Trazabilidad por camino

- `PATH` `PATH-00047` — evidencia: (sin referencias de evidencia)
  - `PATH-00047` → `index/functional_paths.json#PATH-00047`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00047`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00470: proyectos\WebWPF\Formulario470.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario470.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_470` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00470`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_470`).

### Trazabilidad por camino

- `PATH` `PATH-00470` — evidencia: (sin referencias de evidencia)
  - `PATH-00470` → `index/functional_paths.json#PATH-00470`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00470`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00471: proyectos\WebWPF\Formulario471.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario471.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_471` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00471`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_471`).

### Trazabilidad por camino

- `PATH` `PATH-00471` — evidencia: (sin referencias de evidencia)
  - `PATH-00471` → `index/functional_paths.json#PATH-00471`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00471`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00472: proyectos\WebWPF\Formulario472.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario472.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_472` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00472`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_472`).

### Trazabilidad por camino

- `PATH` `PATH-00472` — evidencia: (sin referencias de evidencia)
  - `PATH-00472` → `index/functional_paths.json#PATH-00472`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00472`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00473: proyectos\WebWPF\Formulario473.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario473.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_473` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00473`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_473`).

### Trazabilidad por camino

- `PATH` `PATH-00473` — evidencia: (sin referencias de evidencia)
  - `PATH-00473` → `index/functional_paths.json#PATH-00473`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00473`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00474: proyectos\WebWPF\Formulario474.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario474.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_474` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00474`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_474`).

### Trazabilidad por camino

- `PATH` `PATH-00474` — evidencia: (sin referencias de evidencia)
  - `PATH-00474` → `index/functional_paths.json#PATH-00474`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00474`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00475: proyectos\WebWPF\Formulario475.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario475.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_475` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00475`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_475`).

### Trazabilidad por camino

- `PATH` `PATH-00475` — evidencia: (sin referencias de evidencia)
  - `PATH-00475` → `index/functional_paths.json#PATH-00475`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00475`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00476: proyectos\WebWPF\Formulario476.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario476.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_476` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00476`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_476`).

### Trazabilidad por camino

- `PATH` `PATH-00476` — evidencia: (sin referencias de evidencia)
  - `PATH-00476` → `index/functional_paths.json#PATH-00476`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00476`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00477: proyectos\WebWPF\Formulario477.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario477.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_477` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00477`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_477`).

### Trazabilidad por camino

- `PATH` `PATH-00477` — evidencia: (sin referencias de evidencia)
  - `PATH-00477` → `index/functional_paths.json#PATH-00477`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00477`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00478: proyectos\WebWPF\Formulario478.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario478.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_478` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00478`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_478`).

### Trazabilidad por camino

- `PATH` `PATH-00478` — evidencia: (sin referencias de evidencia)
  - `PATH-00478` → `index/functional_paths.json#PATH-00478`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00478`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00479: proyectos\WebWPF\Formulario479.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario479.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_479` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00479`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_479`).

### Trazabilidad por camino

- `PATH` `PATH-00479` — evidencia: (sin referencias de evidencia)
  - `PATH-00479` → `index/functional_paths.json#PATH-00479`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00479`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00048: proyectos\WebWPF\Formulario48.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario48.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_48` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00048`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_48`).

### Trazabilidad por camino

- `PATH` `PATH-00048` — evidencia: (sin referencias de evidencia)
  - `PATH-00048` → `index/functional_paths.json#PATH-00048`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00048`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00480: proyectos\WebWPF\Formulario480.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario480.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_480` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00480`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_480`).

### Trazabilidad por camino

- `PATH` `PATH-00480` — evidencia: (sin referencias de evidencia)
  - `PATH-00480` → `index/functional_paths.json#PATH-00480`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00480`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00481: proyectos\WebWPF\Formulario481.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario481.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_481` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00481`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_481`).

### Trazabilidad por camino

- `PATH` `PATH-00481` — evidencia: (sin referencias de evidencia)
  - `PATH-00481` → `index/functional_paths.json#PATH-00481`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00481`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00482: proyectos\WebWPF\Formulario482.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario482.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_482` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00482`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_482`).

### Trazabilidad por camino

- `PATH` `PATH-00482` — evidencia: (sin referencias de evidencia)
  - `PATH-00482` → `index/functional_paths.json#PATH-00482`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00482`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00483: proyectos\WebWPF\Formulario483.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario483.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_483` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00483`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_483`).

### Trazabilidad por camino

- `PATH` `PATH-00483` — evidencia: (sin referencias de evidencia)
  - `PATH-00483` → `index/functional_paths.json#PATH-00483`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00483`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00484: proyectos\WebWPF\Formulario484.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario484.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_484` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00484`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_484`).

### Trazabilidad por camino

- `PATH` `PATH-00484` — evidencia: (sin referencias de evidencia)
  - `PATH-00484` → `index/functional_paths.json#PATH-00484`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00484`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00485: proyectos\WebWPF\Formulario485.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario485.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_485` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00485`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_485`).

### Trazabilidad por camino

- `PATH` `PATH-00485` — evidencia: (sin referencias de evidencia)
  - `PATH-00485` → `index/functional_paths.json#PATH-00485`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00485`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00486: proyectos\WebWPF\Formulario486.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario486.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_486` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00486`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_486`).

### Trazabilidad por camino

- `PATH` `PATH-00486` — evidencia: (sin referencias de evidencia)
  - `PATH-00486` → `index/functional_paths.json#PATH-00486`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00486`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00487: proyectos\WebWPF\Formulario487.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario487.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_487` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00487`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_487`).

### Trazabilidad por camino

- `PATH` `PATH-00487` — evidencia: (sin referencias de evidencia)
  - `PATH-00487` → `index/functional_paths.json#PATH-00487`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00487`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00488: proyectos\WebWPF\Formulario488.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario488.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_488` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00488`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_488`).

### Trazabilidad por camino

- `PATH` `PATH-00488` — evidencia: (sin referencias de evidencia)
  - `PATH-00488` → `index/functional_paths.json#PATH-00488`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00488`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00489: proyectos\WebWPF\Formulario489.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario489.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_489` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00489`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_489`).

### Trazabilidad por camino

- `PATH` `PATH-00489` — evidencia: (sin referencias de evidencia)
  - `PATH-00489` → `index/functional_paths.json#PATH-00489`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00489`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00049: proyectos\WebWPF\Formulario49.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario49.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_49` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00049`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_49`).

### Trazabilidad por camino

- `PATH` `PATH-00049` — evidencia: (sin referencias de evidencia)
  - `PATH-00049` → `index/functional_paths.json#PATH-00049`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00049`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00490: proyectos\WebWPF\Formulario490.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario490.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_490` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00490`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_490`).

### Trazabilidad por camino

- `PATH` `PATH-00490` — evidencia: (sin referencias de evidencia)
  - `PATH-00490` → `index/functional_paths.json#PATH-00490`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00490`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00491: proyectos\WebWPF\Formulario491.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario491.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_491` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00491`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_491`).

### Trazabilidad por camino

- `PATH` `PATH-00491` — evidencia: (sin referencias de evidencia)
  - `PATH-00491` → `index/functional_paths.json#PATH-00491`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00491`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00492: proyectos\WebWPF\Formulario492.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario492.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_492` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00492`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_492`).

### Trazabilidad por camino

- `PATH` `PATH-00492` — evidencia: (sin referencias de evidencia)
  - `PATH-00492` → `index/functional_paths.json#PATH-00492`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00492`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00493: proyectos\WebWPF\Formulario493.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario493.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_493` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00493`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_493`).

### Trazabilidad por camino

- `PATH` `PATH-00493` — evidencia: (sin referencias de evidencia)
  - `PATH-00493` → `index/functional_paths.json#PATH-00493`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00493`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00494: proyectos\WebWPF\Formulario494.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario494.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_494` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00494`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_494`).

### Trazabilidad por camino

- `PATH` `PATH-00494` — evidencia: (sin referencias de evidencia)
  - `PATH-00494` → `index/functional_paths.json#PATH-00494`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00494`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00495: proyectos\WebWPF\Formulario495.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario495.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_495` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00495`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_495`).

### Trazabilidad por camino

- `PATH` `PATH-00495` — evidencia: (sin referencias de evidencia)
  - `PATH-00495` → `index/functional_paths.json#PATH-00495`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00495`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00496: proyectos\WebWPF\Formulario496.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario496.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_496` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00496`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_496`).

### Trazabilidad por camino

- `PATH` `PATH-00496` — evidencia: (sin referencias de evidencia)
  - `PATH-00496` → `index/functional_paths.json#PATH-00496`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00496`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00497: proyectos\WebWPF\Formulario497.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario497.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_497` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00497`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_497`).

### Trazabilidad por camino

- `PATH` `PATH-00497` — evidencia: (sin referencias de evidencia)
  - `PATH-00497` → `index/functional_paths.json#PATH-00497`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00497`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00498: proyectos\WebWPF\Formulario498.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario498.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_498` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00498`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_498`).

### Trazabilidad por camino

- `PATH` `PATH-00498` — evidencia: (sin referencias de evidencia)
  - `PATH-00498` → `index/functional_paths.json#PATH-00498`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00498`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00499: proyectos\WebWPF\Formulario499.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario499.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_499` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00499`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_499`).

### Trazabilidad por camino

- `PATH` `PATH-00499` — evidencia: (sin referencias de evidencia)
  - `PATH-00499` → `index/functional_paths.json#PATH-00499`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00499`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00005: proyectos\WebWPF\Formulario5.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario5.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_5` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00005`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_5`).

### Trazabilidad por camino

- `PATH` `PATH-00005` — evidencia: (sin referencias de evidencia)
  - `PATH-00005` → `index/functional_paths.json#PATH-00005`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00005`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00050: proyectos\WebWPF\Formulario50.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario50.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_50` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00050`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_50`).

### Trazabilidad por camino

- `PATH` `PATH-00050` — evidencia: (sin referencias de evidencia)
  - `PATH-00050` → `index/functional_paths.json#PATH-00050`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00050`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00500: proyectos\WebWPF\Formulario500.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario500.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_500` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00500`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_500`).

### Trazabilidad por camino

- `PATH` `PATH-00500` — evidencia: (sin referencias de evidencia)
  - `PATH-00500` → `index/functional_paths.json#PATH-00500`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00500`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00501: proyectos\WebWPF\Formulario501.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario501.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_501` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00501`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_501`).

### Trazabilidad por camino

- `PATH` `PATH-00501` — evidencia: (sin referencias de evidencia)
  - `PATH-00501` → `index/functional_paths.json#PATH-00501`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00501`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00502: proyectos\WebWPF\Formulario502.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario502.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_502` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00502`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_502`).

### Trazabilidad por camino

- `PATH` `PATH-00502` — evidencia: (sin referencias de evidencia)
  - `PATH-00502` → `index/functional_paths.json#PATH-00502`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00502`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00503: proyectos\WebWPF\Formulario503.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario503.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_503` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00503`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_503`).

### Trazabilidad por camino

- `PATH` `PATH-00503` — evidencia: (sin referencias de evidencia)
  - `PATH-00503` → `index/functional_paths.json#PATH-00503`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00503`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00504: proyectos\WebWPF\Formulario504.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario504.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_504` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00504`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_504`).

### Trazabilidad por camino

- `PATH` `PATH-00504` — evidencia: (sin referencias de evidencia)
  - `PATH-00504` → `index/functional_paths.json#PATH-00504`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00504`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00505: proyectos\WebWPF\Formulario505.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario505.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_505` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00505`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_505`).

### Trazabilidad por camino

- `PATH` `PATH-00505` — evidencia: (sin referencias de evidencia)
  - `PATH-00505` → `index/functional_paths.json#PATH-00505`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00505`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00506: proyectos\WebWPF\Formulario506.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario506.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_506` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00506`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_506`).

### Trazabilidad por camino

- `PATH` `PATH-00506` — evidencia: (sin referencias de evidencia)
  - `PATH-00506` → `index/functional_paths.json#PATH-00506`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00506`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00507: proyectos\WebWPF\Formulario507.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario507.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_507` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00507`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_507`).

### Trazabilidad por camino

- `PATH` `PATH-00507` — evidencia: (sin referencias de evidencia)
  - `PATH-00507` → `index/functional_paths.json#PATH-00507`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00507`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00508: proyectos\WebWPF\Formulario508.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario508.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_508` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00508`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_508`).

### Trazabilidad por camino

- `PATH` `PATH-00508` — evidencia: (sin referencias de evidencia)
  - `PATH-00508` → `index/functional_paths.json#PATH-00508`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00508`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00509: proyectos\WebWPF\Formulario509.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario509.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_509` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00509`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_509`).

### Trazabilidad por camino

- `PATH` `PATH-00509` — evidencia: (sin referencias de evidencia)
  - `PATH-00509` → `index/functional_paths.json#PATH-00509`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00509`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00051: proyectos\WebWPF\Formulario51.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario51.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_51` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00051`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_51`).

### Trazabilidad por camino

- `PATH` `PATH-00051` — evidencia: (sin referencias de evidencia)
  - `PATH-00051` → `index/functional_paths.json#PATH-00051`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00051`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00510: proyectos\WebWPF\Formulario510.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario510.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_510` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00510`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_510`).

### Trazabilidad por camino

- `PATH` `PATH-00510` — evidencia: (sin referencias de evidencia)
  - `PATH-00510` → `index/functional_paths.json#PATH-00510`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00510`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00511: proyectos\WebWPF\Formulario511.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario511.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_511` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00511`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_511`).

### Trazabilidad por camino

- `PATH` `PATH-00511` — evidencia: (sin referencias de evidencia)
  - `PATH-00511` → `index/functional_paths.json#PATH-00511`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00511`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00052: proyectos\WebWPF\Formulario52.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario52.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_52` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00052`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_52`).

### Trazabilidad por camino

- `PATH` `PATH-00052` — evidencia: (sin referencias de evidencia)
  - `PATH-00052` → `index/functional_paths.json#PATH-00052`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00052`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00053: proyectos\WebWPF\Formulario53.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario53.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_53` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00053`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_53`).

### Trazabilidad por camino

- `PATH` `PATH-00053` — evidencia: (sin referencias de evidencia)
  - `PATH-00053` → `index/functional_paths.json#PATH-00053`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00053`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00054: proyectos\WebWPF\Formulario54.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario54.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_54` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00054`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_54`).

### Trazabilidad por camino

- `PATH` `PATH-00054` — evidencia: (sin referencias de evidencia)
  - `PATH-00054` → `index/functional_paths.json#PATH-00054`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00054`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00055: proyectos\WebWPF\Formulario55.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario55.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_55` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00055`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_55`).

### Trazabilidad por camino

- `PATH` `PATH-00055` — evidencia: (sin referencias de evidencia)
  - `PATH-00055` → `index/functional_paths.json#PATH-00055`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00055`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00056: proyectos\WebWPF\Formulario56.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario56.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_56` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00056`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_56`).

### Trazabilidad por camino

- `PATH` `PATH-00056` — evidencia: (sin referencias de evidencia)
  - `PATH-00056` → `index/functional_paths.json#PATH-00056`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00056`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00057: proyectos\WebWPF\Formulario57.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario57.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_57` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00057`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_57`).

### Trazabilidad por camino

- `PATH` `PATH-00057` — evidencia: (sin referencias de evidencia)
  - `PATH-00057` → `index/functional_paths.json#PATH-00057`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00057`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00058: proyectos\WebWPF\Formulario58.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario58.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_58` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00058`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_58`).

### Trazabilidad por camino

- `PATH` `PATH-00058` — evidencia: (sin referencias de evidencia)
  - `PATH-00058` → `index/functional_paths.json#PATH-00058`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00058`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00059: proyectos\WebWPF\Formulario59.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario59.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_59` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00059`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_59`).

### Trazabilidad por camino

- `PATH` `PATH-00059` — evidencia: (sin referencias de evidencia)
  - `PATH-00059` → `index/functional_paths.json#PATH-00059`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00059`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00006: proyectos\WebWPF\Formulario6.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario6.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_6` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00006`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_6`).

### Trazabilidad por camino

- `PATH` `PATH-00006` — evidencia: (sin referencias de evidencia)
  - `PATH-00006` → `index/functional_paths.json#PATH-00006`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00006`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00060: proyectos\WebWPF\Formulario60.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario60.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_60` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00060`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_60`).

### Trazabilidad por camino

- `PATH` `PATH-00060` — evidencia: (sin referencias de evidencia)
  - `PATH-00060` → `index/functional_paths.json#PATH-00060`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00060`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00061: proyectos\WebWPF\Formulario61.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario61.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_61` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00061`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_61`).

### Trazabilidad por camino

- `PATH` `PATH-00061` — evidencia: (sin referencias de evidencia)
  - `PATH-00061` → `index/functional_paths.json#PATH-00061`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00061`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00062: proyectos\WebWPF\Formulario62.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario62.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_62` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00062`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_62`).

### Trazabilidad por camino

- `PATH` `PATH-00062` — evidencia: (sin referencias de evidencia)
  - `PATH-00062` → `index/functional_paths.json#PATH-00062`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00062`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00063: proyectos\WebWPF\Formulario63.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario63.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_63` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00063`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_63`).

### Trazabilidad por camino

- `PATH` `PATH-00063` — evidencia: (sin referencias de evidencia)
  - `PATH-00063` → `index/functional_paths.json#PATH-00063`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00063`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00064: proyectos\WebWPF\Formulario64.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario64.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_64` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00064`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_64`).

### Trazabilidad por camino

- `PATH` `PATH-00064` — evidencia: (sin referencias de evidencia)
  - `PATH-00064` → `index/functional_paths.json#PATH-00064`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00064`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00065: proyectos\WebWPF\Formulario65.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario65.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_65` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00065`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_65`).

### Trazabilidad por camino

- `PATH` `PATH-00065` — evidencia: (sin referencias de evidencia)
  - `PATH-00065` → `index/functional_paths.json#PATH-00065`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00065`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00066: proyectos\WebWPF\Formulario66.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario66.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_66` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00066`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_66`).

### Trazabilidad por camino

- `PATH` `PATH-00066` — evidencia: (sin referencias de evidencia)
  - `PATH-00066` → `index/functional_paths.json#PATH-00066`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00066`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00067: proyectos\WebWPF\Formulario67.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario67.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_67` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00067`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_67`).

### Trazabilidad por camino

- `PATH` `PATH-00067` — evidencia: (sin referencias de evidencia)
  - `PATH-00067` → `index/functional_paths.json#PATH-00067`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00067`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00068: proyectos\WebWPF\Formulario68.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario68.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_68` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00068`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_68`).

### Trazabilidad por camino

- `PATH` `PATH-00068` — evidencia: (sin referencias de evidencia)
  - `PATH-00068` → `index/functional_paths.json#PATH-00068`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00068`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00069: proyectos\WebWPF\Formulario69.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario69.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_69` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00069`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_69`).

### Trazabilidad por camino

- `PATH` `PATH-00069` — evidencia: (sin referencias de evidencia)
  - `PATH-00069` → `index/functional_paths.json#PATH-00069`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00069`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00007: proyectos\WebWPF\Formulario7.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario7.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_7` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00007`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_7`).

### Trazabilidad por camino

- `PATH` `PATH-00007` — evidencia: (sin referencias de evidencia)
  - `PATH-00007` → `index/functional_paths.json#PATH-00007`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00007`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00070: proyectos\WebWPF\Formulario70.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario70.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_70` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00070`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_70`).

### Trazabilidad por camino

- `PATH` `PATH-00070` — evidencia: (sin referencias de evidencia)
  - `PATH-00070` → `index/functional_paths.json#PATH-00070`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00070`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00071: proyectos\WebWPF\Formulario71.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario71.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_71` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00071`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_71`).

### Trazabilidad por camino

- `PATH` `PATH-00071` — evidencia: (sin referencias de evidencia)
  - `PATH-00071` → `index/functional_paths.json#PATH-00071`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00071`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00072: proyectos\WebWPF\Formulario72.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario72.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_72` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00072`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_72`).

### Trazabilidad por camino

- `PATH` `PATH-00072` — evidencia: (sin referencias de evidencia)
  - `PATH-00072` → `index/functional_paths.json#PATH-00072`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00072`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00073: proyectos\WebWPF\Formulario73.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario73.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_73` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00073`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_73`).

### Trazabilidad por camino

- `PATH` `PATH-00073` — evidencia: (sin referencias de evidencia)
  - `PATH-00073` → `index/functional_paths.json#PATH-00073`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00073`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00074: proyectos\WebWPF\Formulario74.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario74.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_74` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00074`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_74`).

### Trazabilidad por camino

- `PATH` `PATH-00074` — evidencia: (sin referencias de evidencia)
  - `PATH-00074` → `index/functional_paths.json#PATH-00074`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00074`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00075: proyectos\WebWPF\Formulario75.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario75.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_75` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00075`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_75`).

### Trazabilidad por camino

- `PATH` `PATH-00075` — evidencia: (sin referencias de evidencia)
  - `PATH-00075` → `index/functional_paths.json#PATH-00075`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00075`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00076: proyectos\WebWPF\Formulario76.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario76.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_76` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00076`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_76`).

### Trazabilidad por camino

- `PATH` `PATH-00076` — evidencia: (sin referencias de evidencia)
  - `PATH-00076` → `index/functional_paths.json#PATH-00076`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00076`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00077: proyectos\WebWPF\Formulario77.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario77.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_77` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00077`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_77`).

### Trazabilidad por camino

- `PATH` `PATH-00077` — evidencia: (sin referencias de evidencia)
  - `PATH-00077` → `index/functional_paths.json#PATH-00077`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00077`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00078: proyectos\WebWPF\Formulario78.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario78.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_78` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00078`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_78`).

### Trazabilidad por camino

- `PATH` `PATH-00078` — evidencia: (sin referencias de evidencia)
  - `PATH-00078` → `index/functional_paths.json#PATH-00078`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00078`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00079: proyectos\WebWPF\Formulario79.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario79.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_79` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00079`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_79`).

### Trazabilidad por camino

- `PATH` `PATH-00079` — evidencia: (sin referencias de evidencia)
  - `PATH-00079` → `index/functional_paths.json#PATH-00079`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00079`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00008: proyectos\WebWPF\Formulario8.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario8.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_8` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00008`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_8`).

### Trazabilidad por camino

- `PATH` `PATH-00008` — evidencia: (sin referencias de evidencia)
  - `PATH-00008` → `index/functional_paths.json#PATH-00008`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00008`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00080: proyectos\WebWPF\Formulario80.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario80.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_80` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00080`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_80`).

### Trazabilidad por camino

- `PATH` `PATH-00080` — evidencia: (sin referencias de evidencia)
  - `PATH-00080` → `index/functional_paths.json#PATH-00080`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00080`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00081: proyectos\WebWPF\Formulario81.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario81.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_81` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00081`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_81`).

### Trazabilidad por camino

- `PATH` `PATH-00081` — evidencia: (sin referencias de evidencia)
  - `PATH-00081` → `index/functional_paths.json#PATH-00081`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00081`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00082: proyectos\WebWPF\Formulario82.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario82.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_82` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00082`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_82`).

### Trazabilidad por camino

- `PATH` `PATH-00082` — evidencia: (sin referencias de evidencia)
  - `PATH-00082` → `index/functional_paths.json#PATH-00082`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00082`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00083: proyectos\WebWPF\Formulario83.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario83.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_83` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00083`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_83`).

### Trazabilidad por camino

- `PATH` `PATH-00083` — evidencia: (sin referencias de evidencia)
  - `PATH-00083` → `index/functional_paths.json#PATH-00083`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00083`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00084: proyectos\WebWPF\Formulario84.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario84.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_84` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00084`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_84`).

### Trazabilidad por camino

- `PATH` `PATH-00084` — evidencia: (sin referencias de evidencia)
  - `PATH-00084` → `index/functional_paths.json#PATH-00084`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00084`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00085: proyectos\WebWPF\Formulario85.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario85.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_85` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00085`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_85`).

### Trazabilidad por camino

- `PATH` `PATH-00085` — evidencia: (sin referencias de evidencia)
  - `PATH-00085` → `index/functional_paths.json#PATH-00085`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00085`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00086: proyectos\WebWPF\Formulario86.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario86.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_86` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00086`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_86`).

### Trazabilidad por camino

- `PATH` `PATH-00086` — evidencia: (sin referencias de evidencia)
  - `PATH-00086` → `index/functional_paths.json#PATH-00086`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00086`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00087: proyectos\WebWPF\Formulario87.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario87.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_87` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00087`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_87`).

### Trazabilidad por camino

- `PATH` `PATH-00087` — evidencia: (sin referencias de evidencia)
  - `PATH-00087` → `index/functional_paths.json#PATH-00087`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00087`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

---

# Flujo FLOW-00088: proyectos\WebWPF\Formulario88.ascx → Load

*Documento generado de forma determinista (sin IA), esquema `HUMAN_DOCUMENTATION_PROJECTION 1.0`, modelo `V4.3-R3`.*

## 1. Qué es y dónde está

- Formulario web: `proyectos\WebWPF\Formulario88.ascx`
- Proyectos/capas del sistema involucrados: no determinado
- Confianza general del flujo: confirmado

## 2. Evento/entrada inicial

El flujo se inicia con el evento `Load`, manejado por `Page_Load`.

## 3. Resumen funcional determinista

- 1 camino(s) de ejecución evidenciado(s) en total.
- 0 camino(s) confirmado(s) principal(es) (detalle en la sección 4).
- 1 camino(s) con un límite no resuelto (detalle en la sección 7).
- 0 camino(s) adicional(es) corresponden a infraestructura o código técnico/generado, no a lógica de negocio.

El detalle exhaustivo de todos los caminos -- incluyendo los técnicos/de infraestructura -- está en la sección 8; esta sección resume solo lo funcionalmente relevante.

## 4. Rutas confirmadas principales

No hay caminos confirmados principales distintos de los técnicos/de infraestructura para este flujo (ver el detalle exhaustivo en la sección 8).

## 5. Servicios/capas

No se identificaron servicios/clases de acceso a datos en la cadena de este flujo.

## 6. Datos/SP/SQL

No se confirmó acceso a procedimientos almacenados ni operaciones SQL para este flujo.


## 7. Qué queda no resuelto

- Límite no resuelto: `unknown_88` — no se pudo confirmar su destino real.

Caminos con incertidumbre (`path_id`): `PATH-00088`.

Ninguna de estas ausencias se completa con una suposición: se declaran explícitamente como no resueltas.

## 8. Evidencia técnica detallada / trazabilidad

### Todos los caminos (detalle exhaustivo)

- Camino no resuelto: (sin pasos intermedios evidenciados) termina en límite no resuelto sin nombre resuelto (`unknown_88`).

### Trazabilidad por camino

- `PATH` `PATH-00088` — evidencia: (sin referencias de evidencia)
  - `PATH-00088` → `index/functional_paths.json#PATH-00088`

Puntero de origen del flujo: `index/functional_flows.json#FLOW-00088`
Índices exhaustivos de origen: `index/functional_flows.json`, `index/functional_paths.json`, `index/entry_points.json`, `index/data_access.json`, `index/stored_procedures.json`, `index/sql_operations.json`, `index/data_parameters.json`

## Límites de esta documentación

- Generada exclusivamente a partir de evidencia determinista ya extraída por LegacyMapper; no se invoca ningún proveedor de IA para producir el contenido anterior.
- Los identificadores técnicos (`FLOW-*`, `PATH-*`, `DAO-*`, `SP-*`, `SQL-*`) se conservan intactos como referencia de trazabilidad, pero no son la explicación principal: la explicación usa nombres resueltos cuando existen.
- De 1 camino(s) determinista(s) original(es), 0 fueron cadenas equivalentes fusionadas (sin pérdida de `path_id` ni evidencia); este documento describe los 1 camino(s) resultantes.
- Ningún elemento marcado como ruido técnico/generado (`technical_noise_candidate`) fue eliminado: permanece visible arriba, solo señalado para no confundirlo con lógica de negocio.
- La evidencia transaccional y el tipo de operación de datos (sección 6) solo se muestran cuando el índice determinista ya los registró explícitamente; nunca se clasifica un procedimiento almacenado como 'escritura' a partir de su nombre.
- Este documento no contiene interpretación de IA salvo que se indique explícitamente en una sección separada, etiquetada `INTERPRETED`, al final.

