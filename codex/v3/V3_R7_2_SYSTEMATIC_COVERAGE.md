TASK=V3-R7_2_SYSTEMATIC_COVERAGE

MODE=IMPLEMENT_AND_EXECUTE

PARENT=V3-R7_1
R7_1_STATUS=V3-R7_1_READY_FOR_HUMAN_REVIEW

NO_R8
NO_AI_KNOWLEDGE
NO_HUMAN_APPROVAL
NO_LEGACY_SOURCE_MODIFICATION
NO_V1_V2_REGENERATION
NO_FULL_RAW_REPOSITORY_SCAN
NO_VALIDATOR_WEAKENING
NO_DIRECT_LLM_MARKDOWN_GENERATION

OBJECTIVE

Expand V3 documentation coverage systematically across the existing V2 model while preserving the successful R7.1 constrained-contract behavior.

Current R7.1 documents are valid but based on bounded samples.

Target:

* broader functional coverage;
* broader technical coverage;
* deterministic partitioning of V2 evidence;
* small LLM contexts;
* deterministic intermediate aggregation;
* bounded final synthesis;
* updated DRAFT documents suitable for human review.

Core strategy:

V2 full deterministic model
-> deterministic coverage planner
-> small ContextPackages
-> validated local assessments
-> deterministic aggregation
-> hierarchical synthesis
-> validated global assessment
-> deterministic Markdown rendering

Python determines coverage.
LLM interprets bounded evidence.
Python aggregates and controls final output.

==================================================
PRECONDITIONS
=============

Required:

V2-R5.1 COMPLETE
V3-R1 APPROVED
V3-R2 APPROVED
V3-R3.2 APPROVED
V3-R4 APPROVED
V3-R5 APPROVED
V3-R6.1 APPROVED
V3-R7.1 APPROVED

Baseline:

131 tests PASS

Existing valid drafts:

output/LEVANTAMIENTO_FUNCIONAL.md
output/LEVANTAMIENTO_TECNICO.md

Treat them as replaceable DRAFT outputs, not approved sources.

==================================================
SOURCE AUTHORITY
================

Use only existing V2 deterministic artifacts as factual authority.

Allowed source classes remain:

DETERMINISTIC_CODE_FACT
AI_INTERPRETATION
UNRESOLVED

Do not introduce approved-document source types.

Do not use previous DRAFT Markdown as authoritative evidence.

The previous drafts may be compared for regression/reporting only.

==================================================
COVERAGE GOAL
=============

Do not attempt one giant context.

Systematically cover as much of the V2 model as possible using bounded partitions.

Primary coverage dimensions:

PROJECTS
SOLUTIONS
WEBFORMS
ENTRY_POINTS
FUNCTIONAL_FLOWS
CALL_RELATIONSHIPS
DATA_ACCESS
STORED_PROCEDURES
SQL
DEPENDENCIES
EXTERNAL_ASSEMBLIES
UNRESOLVED_BOUNDARIES

Coverage must be measured and reported.

==================================================
DETERMINISTIC COVERAGE PLANNER
==============================

Implement a deterministic coverage planner.

Preferred location:

legacy_documenter/documentation/coverage.py

Responsibilities:

1. inventory V2 evidence categories;
2. assign stable coverage units;
3. group related evidence;
4. create bounded batches;
5. guarantee deterministic ordering;
6. record coverage metrics;
7. avoid duplicate work;
8. preserve evidence provenance;
9. avoid sending whole datasets to the LLM.

A coverage unit may represent:

* project cluster;
* solution cluster;
* WebForms cluster;
* functional-flow cluster;
* data-access cluster;
* dependency cluster.

==================================================
PARTITIONING RULES
==================

Partition using deterministic relationships already present in V2.

Preferred grouping evidence:

* same project;
* project-reference relationships;
* same solution;
* WebForm ownership/project relation;
* flows sharing project/entry point;
* data operations linked to the same project/method/flow;
* stored procedures linked to known data operations;
* shared external assembly/dependency relationships.

Do not group based only on semantic similarity generated by AI.

Do not invent project ownership.

==================================================
PROJECT COVERAGE
================

The full project inventory must participate in deterministic planning.

Do not limit the system to the six projects used in R7.1.

For every known project, deterministically classify its coverage state:

COVERED
PARTIALLY_COVERED
NO_USABLE_EVIDENCE
UNRESOLVED_OWNERSHIP

The LLM does not decide whether a project was included.

==================================================
FUNCTIONAL COVERAGE
===================

Functional interpretation should prioritize:

project
-> WebForms / entry points
-> handlers
-> flows
-> downstream calls
-> data operation when linked

Goal:

derive bounded evidence-backed functional-area candidates.

A functional-area candidate may aggregate multiple projects only when deterministic relationships or repeated evidence support the grouping.

If grouping relies on names:

INTERPRETED only.

==================================================
TECHNICAL COVERAGE
==================

Technical interpretation should systematically cover:

solution/project organization
project references
external references
WebForms structure
BL/SYS relationships
call graph
data-access styles
stored procedures / SQL
cross-project dependencies
unresolved boundaries

Do not infer architecture solely from naming.

==================================================
DATA ACCESS EXPANSION
=====================

R7.1 used mostly aggregate data-access counts.

R7.2 must use detailed V2 data-access relationships where available.

Attempt deterministic linkage:

project
-> method
-> data-access operation
-> stored procedure / SQL
-> parameters

Do not ask the human for information already present in V2.

Only emit MissingInformation after deterministic lookup has failed or remained ambiguous.

==================================================
STORED PROCEDURE COVERAGE
=========================

Do not send all 5k+ procedures to the LLM.

Partition them by deterministic ownership/usage relationships.

For each procedure classify:

LINKED_TO_COMPONENT
LINKED_TO_FLOW
LINKED_TO_METHOD
UNLINKED
AMBIGUOUS

Only send representative/related procedure evidence inside a bounded package.

Preserve counts separately through deterministic aggregation.

==================================================
UNRESOLVED BOUNDARIES
=====================

Do not discard unresolved relationships.

Classify them deterministically by context:

LOCAL_CALL_UNRESOLVED
CROSS_PROJECT_UNRESOLVED
FRAMEWORK_CALL
UI_BINDING
DATA_BOUNDARY
UNKNOWN

Use existing V2 evidence only.

If exact classification is unsupported, keep UNKNOWN.

Do not let unresolved volume dominate model context.

Send summarized counts plus representative evidence IDs.

==================================================
BATCH SIZE
==========

Preserve R7.1 compliance behavior.

Target per LLM request:

<= 40 evidence/context records

Recommended:

20-35 records

Estimated token target:

<= 5,000 input tokens per local interpretation call

Mandatory P0 evidence retained.

If a batch exceeds limits:

split deterministically.

==================================================
HIERARCHICAL INTERPRETATION
===========================

Use two levels.

LEVEL 1 — LOCAL ASSESSMENTS

For each bounded coverage batch:

ContextPackage
-> Functional or Technical Assessment
-> AssessmentValidator

No Markdown.

LEVEL 2 — GLOBAL SYNTHESIS

Do NOT send raw V2 evidence again.

Construct compact deterministic synthesis packages from VALIDATED local assessments.

Global synthesis must preserve:

claim IDs
claim statuses
evidence refs
package refs
source snapshots
coverage metrics
missing information

Then perform bounded functional and technical synthesis.

==================================================
GLOBAL SYNTHESIS CONTRACT
=========================

Global LLM synthesis must use the same R7.1 canonical contract enforcement.

Do not create a looser summary prompt.

No status promotion.

A global CONFIRMED statement requires deterministic factual support through underlying validated evidence.

An INTERPRETED local claim remains INTERPRETED unless independent deterministic evidence permits CONFIRMED under existing validator rules.

UNRESOLVED remains unresolved unless actual evidence resolves it.

==================================================
CALL BUDGET
===========

Copilot Free quota must be conserved.

Do not create one call per project or flow.

Primary target:

<= 20 real calls total

Hard maximum:

30 real primary calls

Suggested allocation:

8-12 functional local assessments
6-10 technical local assessments
1 functional global synthesis
1 technical global synthesis

Use fewer calls whenever deterministic grouping permits.

No retries for semantic validation failures.

Maximum one retry only for retryable transport / malformed-response failure.

==================================================
COVERAGE PRIORITIZATION
=======================

Prioritize evidence that maximizes system coverage.

Suggested deterministic priority:

P0:
system summaries, project topology, mandatory identity/provenance

P1:
projects with WebForms + flows + data access

P2:
projects with WebForms or data access but incomplete flow linkage

P3:
projects with dependencies only

P4:
isolated/unresolved evidence

Coverage planner must report how many units from each priority were processed.

==================================================
REPRESENTATIVE EVIDENCE
=======================

When a category is too large:

send aggregate deterministic metrics plus bounded representative records.

Representative selection must be deterministic.

Possible criteria:

highest linked-flow count
highest confirmed-call count
highest data-operation count
cross-project linkage
presence of direct-provider access
presence of stored procedures
presence of unresolved boundaries

Do not select randomly.

==================================================
MISSING INFORMATION POLICY
==========================

Before creating MissingInformation:

1. search existing V2 relationships;
2. inspect deterministic traceability;
3. check related project/flow/data-access packages;
4. verify that evidence is genuinely absent/ambiguous.

Do not ask the user for facts LegacyMapper already knows.

Deduplicate MissingInformation across local assessments.

If multiple local requests refer to the same unresolved issue:

merge deterministically.

==================================================
FUNCTIONAL DOCUMENT TARGET
==========================

Regenerate:

output/LEVANTAMIENTO_FUNCIONAL.md

Required sections remain:

1 Metadata
2 Alcance del levantamiento
3 Resumen funcional del aplicativo
4 Módulos o áreas funcionales identificadas
5 Funcionalidades por módulo/área
6 Pantallas, WebForms o puntos de entrada relevantes
7 Flujos funcionales identificados
8 Integraciones funcionales detectadas
9 Operaciones de datos relacionadas con funcionalidades
10 Dependencias funcionales relevantes
11 Información no determinada
12 Solicitudes de información adicional
13 Cobertura del levantamiento
14 Trazabilidad
15 Estado de revisión

==================================================
FUNCTIONAL DOCUMENT QUALITY
===========================

The regenerated document should improve on R7.1 by:

* covering substantially more than six projects;
* identifying supported functional-area candidates;
* associating more WebForms/entry points with areas;
* associating known data operations where V2 supports them;
* reducing false MissingInformation caused only by sample limitation;
* distinguishing system-wide coverage from representative examples.

Do not force every project into a business module.

==================================================
TECHNICAL DOCUMENT TARGET
=========================

Regenerate:

output/LEVANTAMIENTO_TECNICO.md

Required sections:

1 Metadata
2 Alcance técnico
3 Resumen tecnológico
4 Organización de soluciones y proyectos
5 Componentes técnicos identificados
6 Dependencias entre componentes
7 WebForms y capa de presentación
8 Lógica de aplicación / negocio
9 Acceso a datos
10 Oracle / procedimientos almacenados / SQL
11 Flujos técnicos representativos
12 Dependencias entre proyectos
13 Dependencias externas y ensamblados
14 Patrón de diseño / arquitectura
15 Evidencia a favor del patrón
16 Evidencia contradictoria o ambigua
17 Riesgos técnicos observables
18 Información técnica no determinada
19 Solicitudes de información adicional
20 Cobertura técnica
21 Trazabilidad
22 Estado de revisión

==================================================
ARCHITECTURE PATTERN
====================

Do not force a pattern.

Evaluate architecture only after broader technical coverage exists.

Allowed final outcomes:

CONFIRMED
INTERPRETED
NO_PATTERN_CONFIRMED
INSUFFICIENT_EVIDENCE

If Web/bl/sys relationships become broadly supported across independent project groups, they may increase confidence in a layered interpretation.

However:

project prefixes alone remain insufficient.

Include contradictory evidence and exceptions.

==================================================
COVERAGE METRICS
================

Both documents must include deterministic coverage metrics.

At minimum:

total_projects
covered_projects
partially_covered_projects
projects_without_usable_evidence
total_webforms
represented_webforms
total_flows
represented_flows
total_data_operations
linked_data_operations
total_stored_procedures
linked_stored_procedures
unresolved_relationships
represented_unresolved_relationships

Do not claim 100% semantic coverage from structural inclusion.

Separate:

STRUCTURAL_COVERAGE
INTERPRETATION_COVERAGE

==================================================
DOCUMENT STATUS
===============

Both remain:

document_status=DRAFT
human_review_required=true
approved=false
knowledge_source_eligible=false

End:

STATUS=DRAFT
HUMAN_REVIEW_REQUIRED=true
APPROVED=false
AI_KNOWLEDGE_ALLOWED=false

==================================================
TRACEABILITY
============

Preserve hierarchical traceability:

global_claim
-> local_claims
-> context_packages
-> evidence_ids
-> source_snapshots

No dangling references.

Local assessments need not be rendered fully in Markdown, but traceability must remain machine-resolvable.

==================================================
IMPLEMENTATION
==============

Preferred new/modified modules:

legacy_documenter/documentation/coverage.py
legacy_documenter/documentation/generator.py
legacy_documenter/documentation/aggregation.py
legacy_documenter/documentation/renderer.py

Reuse R7.1 contract enforcement.

Do not duplicate interpretation contracts.

Optional persisted intermediate machine artifact:

output/v3_r7_2/COVERAGE_INDEX.json
output/v3_r7_2/LOCAL_ASSESSMENTS.json

Only create these if useful for deterministic traceability/reuse.

Do not create extra human reports.

==================================================
OFFLINE TESTS
=============

Add:

tests/test_v3_r7_2.py

Minimum tests:

1 deterministic project coverage planning
2 all known projects classified
3 deterministic batch splitting
4 batch <= configured record limit
5 mandatory P0 preserved
6 project grouping by deterministic links
7 no AI-based grouping in planner
8 WebForm coverage accounting
9 flow coverage accounting
10 data-operation coverage accounting
11 stored-procedure coverage accounting
12 unresolved coverage accounting
13 deterministic representative selection
14 duplicate coverage unit prevention
15 local assessment validation required
16 invalid local assessment excluded
17 hierarchical aggregation preserves statuses
18 no interpretation promotion
19 MissingInformation deduplication
20 V2 lookup occurs before MissingInformation
21 functional global synthesis input compact
22 technical global synthesis input compact
23 global evidence closure
24 global/local traceability
25 deterministic coverage metrics
26 structural vs interpretation coverage separated
27 architecture pattern not forced
28 DRAFT gate retained
29 AI_KNOWLEDGE blocked
30 repeated offline generation deterministic

All tests offline.

Run:

python -m unittest discover -s tests

Baseline:

131 PASS

Expected:

> 131 PASS

==================================================
REAL EXECUTION
==============

After tests PASS:

run the real systematic R7.2 generation.

Use:

existing V2 artifacts only
CopilotProvider
local authenticated Copilot
R7.1 canonical contract enforcement

Record effective model.

Do not hardcode model name.

==================================================
REAL VALIDATION
===============

PASS requires:

coverage planner executed
local batches bounded
all accepted assessments passed validator
global synthesis validated
both documents generated
coverage metrics emitted
traceability closed
no unsupported status promotion
no invented evidence
no raw-source scan
no legacy modification
AI_KNOWLEDGE still blocked

==================================================
COMPARISON WITH R7.1
====================

Compare regenerated documents with R7.1 at metric level.

Report:

projects represented before/after
flows represented before/after
WebForms represented before/after
data operations linked before/after
stored procedures linked before/after
MissingInformation count before/after
CONFIRMED count before/after
INTERPRETED count before/after
UNRESOLVED count before/after

Do not use previous Markdown as evidence.

Comparison is reporting only.

==================================================
MODEL FAILURE RULE
==================

R7.1 corrected the contract and succeeded.

R7.2 is not automatically MODEL_CONTRACT_ATTEMPT=3.

Only count a model-contract failure if:

request prevalidation PASS
context within bounds
canonical schema supplied
response still violates semantic contract

If three comparable validated-request failures are reached:

MODEL_CHANGE_RECOMMENDED=true

Otherwise false.

==================================================
SECURITY
========

Maintain:

no credentials persisted
no environment dump
no model filesystem tools
no model shell
no model Git
no MCP model actions
no legacy-source writes
no raw auth data
no unrestricted tool permissions

==================================================
SOURCE IMMUTABILITY
===================

Do not modify:

C:\Users\cgalianj\source\IST_40\operacional

No new raw scan.

Read only existing V2 outputs.

==================================================
ACCEPTANCE
==========

SUCCESS requires:

* systematic coverage planner implemented;
* project inventory deterministically classified;
* bounded local assessment strategy implemented;
* hierarchical validated synthesis implemented;
* functional DRAFT regenerated;
* technical DRAFT regenerated;
* coverage materially improved over R7.1;
* MissingInformation generated only after V2 lookup;
* coverage metrics present;
* tests PASS;
* traceability closed;
* source immutable;
* AI_KNOWLEDGE blocked.

Success:

STATUS=V3-R7_2_READY_FOR_HUMAN_REVIEW
DECISION=SYSTEMATIC_COVERAGE_EXPANSION_VALIDATED
NEXT=V3-R8_HUMAN_REVIEW_NOT_STARTED

If implementation succeeds but meaningful coverage does not improve:

STATUS=V3-R7_2_INSUFFICIENT_COVERAGE_GAIN
DECISION=COVERAGE_STRATEGY_REVIEW_REQUIRED

If model contract fails:

STATUS=V3-R7_2_MODEL_CONTRACT_FAILURE

If provider/quota unavailable:

STATUS=V3-R7_2_BLOCKED_PROVIDER

If evidence topology prevents broader linkage:

STATUS=V3-R7_2_V2_LINKAGE_LIMITATION

Do not start R8.

==================================================
REPORT
======

Create ONLY:

codex/V3/V3_R7_2_RESULTADO.md

FORMAT:

STATUS
FILES_CHANGED
PRECONDITIONS
COVERAGE_PLANNER
V2_SOURCE_ARTIFACTS
TOTAL_COVERAGE_UNITS
PROJECT_COVERAGE
SOLUTION_COVERAGE
WEBFORM_COVERAGE
FLOW_COVERAGE
DATA_ACCESS_COVERAGE
STORED_PROCEDURE_COVERAGE
UNRESOLVED_COVERAGE
BATCH_COUNT
BATCH_LIMITS
LOCAL_FUNCTIONAL_ASSESSMENTS
LOCAL_TECHNICAL_ASSESSMENTS
GLOBAL_FUNCTIONAL_SYNTHESIS
GLOBAL_TECHNICAL_SYNTHESIS
SELECTED_PROVIDER
MODEL_ID
REAL_CALLS_EXECUTED
ASSESSMENT_VALIDATION
MISSING_INFORMATION_DEDUPLICATION
FUNCTIONAL_DOCUMENT
TECHNICAL_DOCUMENT
ARCHITECTURE_PATTERN_RESULT
COVERAGE_R7_1_VS_R7_2
TRACEABILITY
UNIT_TESTS
TOTAL_TESTS
REGRESSION
SOURCE_IMMUTABILITY
SECURITY
MODEL_OBSERVATIONS
MODEL_CHANGE_RECOMMENDED
KNOWN_LIMITATIONS
FAILURES
DECISION
NEXT

Stop.